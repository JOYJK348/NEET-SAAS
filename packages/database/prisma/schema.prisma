// ── Auto-generated Prisma Schema ──────────────────────────────
// Generated from SQL files in database/
// Source: 354 SQL files across 21 modules
//
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

// ── Enums ────────────────────────────────────────────────
enum AcademicStatusEnum {
  ACTIVE
  SUSPENDED
  WITHDRAWN
  ALUMNI
}

enum AdjustmentTypeEnum {
  CREDIT
  DEBIT
}

enum AdmissionStatusEnum {
  ENQUIRY
  ACTIVE
  ON_HOLD
  DROPPED
  COMPLETED
  ALUMNI
}

enum AiAuditEventEnum {
  PROVIDER_REGISTERED
  MODEL_TOGGLED
  PROMPT_UPDATED
  CONVERSATION_ARCHIVED
  REQUEST_LOGGED
}

enum AiRequestStatusEnum {
  SUCCESS
  FAILED
  TIMEOUT
}

enum AnalyticsAuditEventEnum {
  DASHBOARD_CREATED
  WIDGET_CONFIGURED
  REPORT_EXECUTED
  KPI_REBUILT
  STUDENT_ANALYTICS_MUTATED
}

enum AnnouncementTypeEnum {
  GENERAL
  HOLIDAY
  EXAM_CIRCULAR
  FEE_CIRCULAR
  PLACEMENT
}

enum AnswerStatusEnum {
  CORRECT
  INCORRECT
  PARTIAL
  UNATTEMPTED
  FLAGGED
}

enum AttachmentTypeEnum {
  IMAGE
  DIAGRAM
  PDF
  AUDIO
  VIDEO
  FORMULA
}

enum AttemptStatusEnum {
  NOT_STARTED
  IN_PROGRESS
  SUBMITTED
  AUTO_SUBMITTED
  ABANDONED
  DISQUALIFIED
}

enum AttendanceModeType {
  CLASSROOM
  ONLINE
  HYBRID
}

enum AttendanceSessionStatusEnum {
  DRAFT
  OPEN
  PUBLISHED
  LOCKED
}

enum AttendanceStatusEnum {
  PRESENT
  ABSENT
  LATE
  HALF_DAY
  EXCUSED
}

enum AuthEventType {
  LOGIN_SUCCESS
  LOGIN_FAILED
  LOGOUT
  TOKEN_REFRESHED
  TOKEN_REPLAY_DETECTED
  PASSWORD_CHANGED
  PASSWORD_RESET_REQUEST
  PASSWORD_RESET_SUCCESS
  FORCE_PASSWORD_CHANGE
  MFA_CHALLENGE
  MFA_SUCCESS
  MFA_FAILED
  SESSION_REVOKED
  ACCOUNT_LOCKED
  ACCOUNT_UNLOCKED
}

enum BackgroundJobStatusEnum {
  PENDING
  RUNNING
  COMPLETED
  FAILED
}

enum BatchStatusType {
  PLANNED
  ACTIVE
  COMPLETED
  CANCELLED
  ARCHIVED
}

enum BloodGroupType {
  A_POS
  A_NEG
  B_POS
  B_NEG
  AB_POS
  AB_NEG
  O_POS
  O_NEG
}

enum BloomsLevelEnum {
  REMEMBER
  UNDERSTAND
  APPLY
  ANALYZE
  EVALUATE
  CREATE
}

enum BranchType {
  HEAD_OFFICE
  CAMPUS
  FRANCHISE
  ONLINE
}

enum CampaignStatusEnum {
  DRAFT
  SCHEDULED
  ACTIVE
  PAUSED
  COMPLETED
  CANCELLED
}

enum ChatMessageRoleEnum {
  SYSTEM
  USER
  ASSISTANT
}

enum ChatMessageTypeEnum {
  TEXT
  IMAGE
  FILE
  LINK
  SYSTEM
}

enum ClockDirectionType {
  IN
  OUT
}

enum ClosureStatusEnum {
  OPEN
  CLOSED
  RECONCILED
}

enum CommsChannelType {
  EMAIL
  SMS
  PUSH
  WHATSAPP
}

enum CommunicationAuditEventTypeEnum {
  TEMPLATE_CREATED
  TEMPLATE_UPDATED
  VERSION_ARCHIVED
  CAMPAIGN_LAUNCHED
  PREFERENCE_UPDATED
  DEVICE_REGISTERED
  PROVIDER_TOGGLED
  FAILOVER_TRIGGERED
}

enum DashboardViewerRoleEnum {
  PRINCIPAL
  FACULTY
  STUDENT
  PARENT
  FINANCE
}

enum DeliveryStatusEnum {
  PENDING
  SENT
  DELIVERED
  READ
  FAILED
  BOUNCED
  SPAM
}

enum DevicePlatformEnum {
  ANDROID
  IOS
  WEB
}

enum DiscussionStatusEnum {
  OPEN
  RESOLVED
  CLOSED
}

enum DocumentCategoryEnum {
  QUESTION_PAPER
  ANSWER_KEY
  OMR_SCAN
  SOLUTION_PDF
  ADMIT_CARD
  HALL_TICKET
  RESULT_SHEET
  OTHER
}

enum DocumentVisibilityType {
  PUBLIC
  PRIVATE
}

enum EmploymentStatusEnum {
  ACTIVE
  ON_NOTICE
  RESIGNED
  TERMINATED
  SUSPENDED
}

enum EmploymentTypeEnum {
  FULL_TIME
  PART_TIME
  CONTRACT
  VISITING
}

enum EvaluationStatusEnum {
  PENDING
  IN_PROGRESS
  COMPLETED
  RE_EVALUATION
}

enum ExamModeEnum {
  ONLINE
  OFFLINE
  HYBRID
}

enum ExamPublishStatusEnum {
  DRAFT
  SCHEDULED
  PUBLISHED
  ARCHIVED
  CANCELLED
}

enum ExamStatusEnum {
  ACTIVE
  INACTIVE
  ARCHIVED
}

enum ExamTypeEnum {
  WEEKLY
  MONTHLY
  GRAND
  FULL_SYLLABUS
  CHAPTER
  UNIT
  REVISION
  PRACTICE
  SCHOLARSHIP
}

enum FeeAuditEventTypeEnum {
  STRUCTURE_CREATED
  STRUCTURE_UPDATED
  FEE_ASSIGNED
  DISCOUNT_APPLIED
  INSTALLMENT_OVERRIDDEN
  PENALTY_ADDED
  PAYMENT_RECEIVED
  PAYMENT_FAILED
  REFUND_PROCESSED
  FEE_WAIVED
  RECONCILIATION_MATCHED
  ADJUSTMENT_APPLIED
  CLOSURE_COMPLETED
}

enum FeeNotificationChannelEnum {
  SMS
  WHATSAPP
  EMAIL
}

enum FeeNotificationStatusEnum {
  PENDING
  SENT
  FAILED
}

enum FeeStatusEnum {
  PAID
  UNPAID
  PARTIAL
  WAIVED
  NOT_APPLICABLE
}

enum FeeStructureStatusEnum {
  ACTIVE
  INACTIVE
  ARCHIVED
}

enum GenderType {
  MALE
  FEMALE
  OTHER
}

enum ImportStatusEnum {
  PENDING
  IN_PROGRESS
  COMPLETED
  FAILED
  PARTIAL
}

enum InstallmentStatusEnum {
  UNPAID
  PARTIALLY_PAID
  PAID
  OVERDUE
  WAIVED
}

enum LearningAttachmentTypeEnum {
  VIDEO
  PDF
  DOC
  PPT
  ZIP
  IMAGE
  SVG
  AUDIO
  SUBTITLE
  THUMBNAIL
  SLIDE
  DIAGRAM
  FORMULA
}

enum LearningMaterialStatusEnum {
  DRAFT
  PUBLISHED
  ARCHIVED
}

enum LearningMaterialTypeEnum {
  NOTES
  VIDEO
  PDF
  DOC
  PPT
  ZIP
  IMAGE
  SVG
  AUDIO
  REVISION_SHEET
  MIND_MAP
  LAB_MANUAL
  FLASH_CARDS
  PRESENTATION
  SLIDE
  SUBTITLE
  THUMBNAIL
  QUIZ
  ASSIGNMENT
}

enum LearningPathStatusEnum {
  ACTIVE
  INACTIVE
  ARCHIVED
}

enum LeaveCategoryEnum {
  SICK
  CASUAL
  EMERGENCY
  DUTY
  OTHER
}

enum LeaveStatusEnum {
  PENDING
  APPROVED
  REJECTED
  CANCELLED
}

enum LiveClassEventTypeEnum {
  HOST_JOINED
  HOST_LEFT
  STUDENT_JOINED
  STUDENT_LEFT
  RECORDING_STARTED
  RECORDING_STOPPED
  POLL_CREATED
  POLL_CLOSED
  BREAKOUT_STARTED
  BREAKOUT_ENDED
  SCREEN_SHARE_STARTED
  WHITEBOARD_OPENED
  CHAT_DISABLED
  MIC_DISABLED
}

enum LiveClassStatusEnum {
  DRAFT
  PUBLISHED
  SCHEDULED
  WAITING
  LIVE
  PAUSED
  ENDED
  CANCELLED
  ARCHIVED
}

enum LiveClassTypeEnum {
  LIVE
  RECORDED
  HYBRID
  DOUBT_SESSION
  REVISION
  CRASH_COURSE
  TEST_DISCUSSION
}

enum MaterialVisibilityEnum {
  PUBLIC
  COURSE_ONLY
  BATCH_ONLY
  PRIVATE
}

enum MeetingProviderEnum {
  ZOOM
  GOOGLE_MEET
  TEAMS
  LMS_INTERNAL
  OTHER
}

enum MenuNodeType {
  MODULE
  GROUP
  MENU
}

enum NotificationChannelTypeEnum {
  SMS
  EMAIL
  WHATSAPP
  PUSH
  IN_APP
}

enum ParentRelationshipTypeEnum {
  FATHER
  MOTHER
  GUARDIAN
  OTHER
}

enum ParticipantRoleEnum {
  HOST
  CO_HOST
  MODERATOR
  GUEST
  OBSERVER
}

enum PaymentMethodEnum {
  CASH
  CARD
  UPI
  BANK_TRANSFER
  CHEQUE
  ONLINE_GATEWAY
}

enum PlatformAuditEventEnum {
  SETTING_MUTATED
  FLAG_TOGGLED
  API_KEY_ROTATED
  JOB_SCHEDULED
  HEALTH_HEALTHY
  ERROR_LOGGED
}

enum PollStatusEnum {
  DRAFT
  ACTIVE
  CLOSED
}

enum PollTypeEnum {
  MCQ
  MULTI_CORRECT
  YES_NO
}

enum ProgressStatusEnum {
  NOT_STARTED
  IN_PROGRESS
  COMPLETED
}

enum QuestionDifficultyEnum {
  VERY_EASY
  EASY
  MEDIUM
  HARD
  VERY_HARD
}

enum QuestionSourceEnum {
  MANUAL
  AI_GENERATED
  IMPORT
  BULK_UPLOAD
  OCR
  SCANNED
}

enum QuestionStatusEnum {
  DRAFT
  REVIEW_PENDING
  APPROVED
  PUBLISHED
  ARCHIVED
}

enum QuestionTypeEnum {
  MCQ
  MULTI_CORRECT
  ASSERTION_REASON
  TRUE_FALSE
  MATCHING
  NUMERICAL
  DESCRIPTIVE
}

enum QueueStatusEnum {
  PENDING
  PROCESSING
  SENT
  FAILED
  RETRY_PENDING
}

enum RaiseHandStatusEnum {
  PENDING
  ACCEPTED
  REJECTED
  RESOLVED
}

enum ReconciliationStatusEnum {
  RECONCILED
  UNMATCHED
  DISCREPANCY
}

enum RecordingStatusEnum {
  PROCESSING
  COMPLETED
  FAILED
  ARCHIVED
}

enum RefreshJobStatusEnum {
  PENDING
  RUNNING
  COMPLETED
  FAILED
}

enum RefundStatusEnum {
  REQUESTED
  APPROVED
  PROCESSED
  REJECTED
}

enum RegistrationStatusEnum {
  REGISTERED
  CONFIRMED
  CANCELLED
  ABSENT
}

enum ReportFormatEnum {
  PDF
  CSV
  XLSX
}

enum ResourceTypeEnum {
  PDF
  PPT
  WORKSHEET
  IMAGE
  VIDEO
  LINK
}

enum ResultStatusEnum {
  PENDING
  EVALUATING
  PUBLISHED
  WITHHELD
  RE_EVALUATED
}

enum ReviewStatusEnum {
  PENDING
  IN_REVIEW
  CHANGES_REQUESTED
  APPROVED
  REJECTED
}

enum RoleAssignmentSource {
  MANUAL
  SYSTEM
  MIGRATION
  API
  IMPORT
}

enum SessionEndReasonEnum {
  COMPLETED
  HOST_LEFT
  CRASHED
  TIMEOUT
  FORCE_CLOSED
}

enum SessionStatusEnum {
  CREATED
  WAITING
  STARTED
  PAUSED
  RESUMED
  ENDED
  FAILED
}

enum SessionStatusType {
  ACTIVE
  EXPIRED
  REVOKED
  LOGGED_OUT
}

enum SessionTypeEnum {
  BATCH
  GROUP
  ONE_TO_ONE
}

enum StorageProviderType {
  LOCAL
  R2
  S3
}

enum TagTypeEnum {
  TOPIC
  CHAPTER
  DIFFICULTY
  EXAM_TYPE
  FREQUENCY
  SOURCE
  CUSTOM
}

enum TransactionStatusEnum {
  PENDING
  SUCCESS
  FAILED
  REVERSED
  REFUNDED
}

enum UserStatusType {
  PENDING
  ACTIVE
  SUSPENDED
  INACTIVE
}

enum UserTypeEnum {
  STAFF
  STUDENT
  PARENT
  TUTOR
  SYSTEM
}

enum WeekdayType {
  MONDAY
  TUESDAY
  WEDNESDAY
  THURSDAY
  FRIDAY
  SATURDAY
  SUNDAY
}

enum WidgetTypeEnum {
  KPI_CARD
  BAR_CHART
  LINE_CHART
  PIE_CHART
  DATA_TABLE
}

// ── Models ───────────────────────────────────────────────
model Institutes {
  id                               String                         @id @default(uuid())
  code                             String
  slug                             String
  name                             String
  displayName                      String
  email                            String
  phone                            String
  website                          String
  logoFileId                       String
  status                           String                         @default("ACTIVE")
  timezone                         String                         @default("Asia/Kolkata")
  currency                         String                         @default("INR")
  createdAt                        DateTime                       @default(now())
  createdBy                        String
  updatedAt                        DateTime                       @default(now())
  updatedBy                        String
  deletedAt                        DateTime
  deletedBy                        String
  version                          Int                            @default(1)
  branchess                        Branches[]                     @relation("tenant_id_to_institutes")
  academic_yearss                  AcademicYears[]                @relation("tenant_id_to_institutes")
  departmentss                     Departments[]                  @relation("tenant_id_to_institutes")
  branch_departmentss              BranchDepartments[]            @relation("tenant_id_to_institutes")
  designationss                    Designations[]                 @relation("tenant_id_to_institutes")
  coursess                         Courses[]                      @relation("tenant_id_to_institutes")
  subjectss                        Subjects[]                     @relation("tenant_id_to_institutes")
  course_subjectss                 CourseSubjects[]               @relation("tenant_id_to_institutes")
  chapterss                        Chapters[]                     @relation("tenant_id_to_institutes")
  topicss                          Topics[]                       @relation("tenant_id_to_institutes")
  batch_delivery_typess            BatchDeliveryTypes[]           @relation("tenant_id_to_institutes")
  batchess                         Batches[]                      @relation("tenant_id_to_institutes")
  userss                           Users[]                        @relation("tenant_id_to_institutes")
  staff_profiless                  StaffProfiles[]                @relation("tenant_id_to_institutes")
  student_profiless                StudentProfiles[]              @relation("tenant_id_to_institutes")
  parent_profiless                 ParentProfiles[]               @relation("tenant_id_to_institutes")
  student_parentss                 StudentParents[]               @relation("tenant_id_to_institutes")
  student_admissionss              StudentAdmissions[]            @relation("tenant_id_to_institutes")
  student_batch_enrollmentss       StudentBatchEnrollments[]      @relation("tenant_id_to_institutes")
  staff_departmentss               StaffDepartments[]             @relation("tenant_id_to_institutes")
  staff_subjectss                  StaffSubjects[]                @relation("tenant_id_to_institutes")
  student_documentss               StudentDocuments[]             @relation("tenant_id_to_institutes")
  staff_batch_assignmentss         StaffBatchAssignments[]        @relation("tenant_id_to_institutes")
  student_medical_profiless        StudentMedicalProfiles[]       @relation("tenant_id_to_institutes")
  staff_qualificationss            StaffQualifications[]          @relation("tenant_id_to_institutes")
  emergency_contactss              EmergencyContacts[]            @relation("tenant_id_to_institutes")
  staff_employment_historys        StaffEmploymentHistory[]       @relation("tenant_id_to_institutes")
  student_status_historys          StudentStatusHistory[]         @relation("tenant_id_to_institutes")
  person_identifierss              PersonIdentifiers[]            @relation("tenant_id_to_institutes")
  attendance_sessionss             AttendanceSessions[]           @relation("tenant_id_to_institutes")
  attendance_recordss              AttendanceRecords[]            @relation("tenant_id_to_institutes")
  leave_requestss                  LeaveRequests[]                @relation("tenant_id_to_institutes")
  attendance_adjustmentss          AttendanceAdjustments[]        @relation("tenant_id_to_institutes")
  leave_attachmentss               LeaveAttachments[]             @relation("tenant_id_to_institutes")
  examss                           Exams[]                        @relation("tenant_id_to_institutes")
  exam_sectionss                   ExamSections[]                 @relation("tenant_id_to_institutes")
  exam_questionss                  ExamQuestions[]                @relation("tenant_id_to_institutes")
  question_paperss                 QuestionPapers[]               @relation("tenant_id_to_institutes")
  exam_registrationss              ExamRegistrations[]            @relation("tenant_id_to_institutes")
  exam_attemptss                   ExamAttempts[]                 @relation("tenant_id_to_institutes")
  exam_answerss                    ExamAnswers[]                  @relation("tenant_id_to_institutes")
  exam_resultss                    ExamResults[]                  @relation("tenant_id_to_institutes")
  exam_result_historys             ExamResultHistory[]            @relation("tenant_id_to_institutes")
  exam_documentss                  ExamDocuments[]                @relation("tenant_id_to_institutes")
  questionss                       Questions[]                    @relation("tenant_id_to_institutes")
  question_optionss                QuestionOptions[]              @relation("tenant_id_to_institutes")
  question_explanationss           QuestionExplanations[]         @relation("tenant_id_to_institutes")
  question_tagss                   QuestionTags[]                 @relation("tenant_id_to_institutes")
  question_tag_mappingss           QuestionTagMappings[]          @relation("tenant_id_to_institutes")
  question_versionss               QuestionVersions[]             @relation("tenant_id_to_institutes")
  question_attachmentss            QuestionAttachments[]          @relation("tenant_id_to_institutes")
  question_reviewss                QuestionReviews[]              @relation("tenant_id_to_institutes")
  question_usages                  QuestionUsage[]                @relation("tenant_id_to_institutes")
  question_import_jobss            QuestionImportJobs[]           @relation("tenant_id_to_institutes")
  learning_materialss              LearningMaterials[]            @relation("tenant_id_to_institutes")
  material_versionss               MaterialVersions[]             @relation("tenant_id_to_institutes")
  material_attachmentss            MaterialAttachments[]          @relation("tenant_id_to_institutes")
  learning_pathss                  LearningPaths[]                @relation("tenant_id_to_institutes")
  learning_path_materialss         LearningPathMaterials[]        @relation("tenant_id_to_institutes")
  student_learning_progresss       StudentLearningProgress[]      @relation("tenant_id_to_institutes")
  learning_bookmarkss              LearningBookmarks[]            @relation("tenant_id_to_institutes")
  learning_notess                  LearningNotes[]                @relation("tenant_id_to_institutes")
  material_discussionss            MaterialDiscussions[]          @relation("tenant_id_to_institutes")
  material_assignmentss            MaterialAssignments[]          @relation("tenant_id_to_institutes")
  assignment_submissionss          AssignmentSubmissions[]        @relation("tenant_id_to_institutes")
  live_classess                    LiveClasses[]                  @relation("tenant_id_to_institutes")
  live_class_instructorss          LiveClassInstructors[]         @relation("tenant_id_to_institutes")
  live_class_sessionss             LiveClassSessions[]            @relation("tenant_id_to_institutes")
  live_class_participantss         LiveClassParticipants[]        @relation("tenant_id_to_institutes")
  live_class_attendances           LiveClassAttendance[]          @relation("tenant_id_to_institutes")
  live_class_recordingss           LiveClassRecordings[]          @relation("tenant_id_to_institutes")
  live_class_resourcess            LiveClassResources[]           @relation("tenant_id_to_institutes")
  live_class_chat_messagess        LiveClassChatMessages[]        @relation("tenant_id_to_institutes")
  live_class_pollss                LiveClassPolls[]               @relation("tenant_id_to_institutes")
  live_class_poll_responsess       LiveClassPollResponses[]       @relation("tenant_id_to_institutes")
  live_class_whiteboard_snapshotss LiveClassWhiteboardSnapshots[] @relation("tenant_id_to_institutes")
  live_class_raise_handss          LiveClassRaiseHands[]          @relation("tenant_id_to_institutes")
  live_class_breakout_roomss       LiveClassBreakoutRooms[]       @relation("tenant_id_to_institutes")
  breakout_room_participantss      BreakoutRoomParticipants[]     @relation("tenant_id_to_institutes")
  live_class_eventss               LiveClassEvents[]              @relation("tenant_id_to_institutes")
  fee_structuress                  FeeStructures[]                @relation("tenant_id_to_institutes")
  fee_structure_itemss             FeeStructureItems[]            @relation("tenant_id_to_institutes")
  student_fee_assignmentss         StudentFeeAssignments[]        @relation("tenant_id_to_institutes")
  fee_installmentss                FeeInstallments[]              @relation("tenant_id_to_institutes")
  student_fee_installmentss        StudentFeeInstallments[]       @relation("tenant_id_to_institutes")
  fee_paymentss                    FeePayments[]                  @relation("tenant_id_to_institutes")
  payment_transactionss            PaymentTransactions[]          @relation("tenant_id_to_institutes")
  payment_refundss                 PaymentRefunds[]               @relation("tenant_id_to_institutes")
  fee_discountss                   FeeDiscounts[]                 @relation("tenant_id_to_institutes")
  student_fee_discountss           StudentFeeDiscounts[]          @relation("tenant_id_to_institutes")
  fee_penaltiess                   FeePenalties[]                 @relation("tenant_id_to_institutes")
  student_fee_penaltiess           StudentFeePenalties[]          @relation("tenant_id_to_institutes")
  fee_receiptss                    FeeReceipts[]                  @relation("tenant_id_to_institutes")
  scholarshipss                    Scholarships[]                 @relation("tenant_id_to_institutes")
  fee_waiverss                     FeeWaivers[]                   @relation("tenant_id_to_institutes")
  payment_reconciliations          PaymentReconciliation[]        @relation("tenant_id_to_institutes")
  fee_notificationss               FeeNotifications[]             @relation("tenant_id_to_institutes")
  fee_audit_logss                  FeeAuditLogs[]                 @relation("tenant_id_to_institutes")
  fee_adjustmentss                 FeeAdjustments[]               @relation("tenant_id_to_institutes")
  payment_allocationss             PaymentAllocations[]           @relation("tenant_id_to_institutes")
  fee_collection_closuress         FeeCollectionClosures[]        @relation("tenant_id_to_institutes")
  fee_collection_centerss          FeeCollectionCenters[]         @relation("tenant_id_to_institutes")
  financial_periods_mappings       FinancialPeriodsMapping[]      @relation("tenant_id_to_institutes")
  notification_templatess          NotificationTemplates[]        @relation("tenant_id_to_institutes")
  notification_template_versionss  NotificationTemplateVersions[] @relation("tenant_id_to_institutes")
  notification_campaignss          NotificationCampaigns[]        @relation("tenant_id_to_institutes")
  notification_campaign_targetss   NotificationCampaignTargets[]  @relation("tenant_id_to_institutes")
  notification_queues              NotificationQueue[]            @relation("tenant_id_to_institutes")
  notification_deliveriess         NotificationDeliveries[]       @relation("tenant_id_to_institutes")
  notification_preferencess        NotificationPreferences[]      @relation("tenant_id_to_institutes")
  notification_subscriptionss      NotificationSubscriptions[]    @relation("tenant_id_to_institutes")
  notification_logss               NotificationLogs[]             @relation("tenant_id_to_institutes")
  sms_providerss                   SmsProviders[]                 @relation("tenant_id_to_institutes")
  email_providerss                 EmailProviders[]               @relation("tenant_id_to_institutes")
  whatsapp_providerss              WhatsappProviders[]            @relation("tenant_id_to_institutes")
  push_devicess                    PushDevices[]                  @relation("tenant_id_to_institutes")
  push_notificationss              PushNotifications[]            @relation("tenant_id_to_institutes")
  announcementss                   Announcements[]                @relation("tenant_id_to_institutes")
  announcement_readss              AnnouncementReads[]            @relation("tenant_id_to_institutes")
  webhook_eventss                  WebhookEvents[]                @relation("tenant_id_to_institutes")
  webhook_deliveriess              WebhookDeliveries[]            @relation("tenant_id_to_institutes")
  communication_audit_logss        CommunicationAuditLogs[]       @relation("tenant_id_to_institutes")
  notification_attachmentss        NotificationAttachments[]      @relation("tenant_id_to_institutes")
  notification_variabless          NotificationVariables[]        @relation("tenant_id_to_institutes")
  notification_retry_queues        NotificationRetryQueue[]       @relation("tenant_id_to_institutes")
  communication_rate_limitss       CommunicationRateLimits[]      @relation("tenant_id_to_institutes")
  provider_failoverss              ProviderFailovers[]            @relation("tenant_id_to_institutes")
  dashboard_definitionss           DashboardDefinitions[]         @relation("tenant_id_to_institutes")
  dashboard_widgetss               DashboardWidgets[]             @relation("tenant_id_to_institutes")
  report_definitionss              ReportDefinitions[]            @relation("tenant_id_to_institutes")
  report_executionss               ReportExecutions[]             @relation("tenant_id_to_institutes")
  kpi_definitionss                 KpiDefinitions[]               @relation("tenant_id_to_institutes")
  kpi_snapshotss                   KpiSnapshots[]                 @relation("tenant_id_to_institutes")
  student_analyticss               StudentAnalytics[]             @relation("tenant_id_to_institutes")
  branch_analyticss                BranchAnalytics[]              @relation("tenant_id_to_institutes")
  financial_analyticss             FinancialAnalytics[]           @relation("tenant_id_to_institutes")
  analytics_refresh_jobss          AnalyticsRefreshJobs[]         @relation("tenant_id_to_institutes")
  analytics_audit_logss            AnalyticsAuditLogs[]           @relation("tenant_id_to_institutes")
  ai_providerss                    AiProviders[]                  @relation("tenant_id_to_institutes")
  ai_modelss                       AiModels[]                     @relation("tenant_id_to_institutes")
  ai_promptss                      AiPrompts[]                    @relation("tenant_id_to_institutes")
  ai_conversationss                AiConversations[]              @relation("tenant_id_to_institutes")
  ai_messagess                     AiMessages[]                   @relation("tenant_id_to_institutes")
  ai_requestss                     AiRequests[]                   @relation("tenant_id_to_institutes")
  ai_generated_contents            AiGeneratedContent[]           @relation("tenant_id_to_institutes")
  ai_feedbacks                     AiFeedback[]                   @relation("tenant_id_to_institutes")
  ai_usage_logss                   AiUsageLogs[]                  @relation("tenant_id_to_institutes")
  ai_audit_logss                   AiAuditLogs[]                  @relation("tenant_id_to_institutes")
  tenant_settingss                 TenantSettings[]               @relation("tenant_id_to_institutes")
  storage_objectss                 StorageObjects[]               @relation("tenant_id_to_institutes")
  file_uploadss                    FileUploads[]                  @relation("tenant_id_to_institutes")
  background_jobss                 BackgroundJobs[]               @relation("tenant_id_to_institutes")
  job_executionss                  JobExecutions[]                @relation("tenant_id_to_institutes")
  scheduler_jobss                  SchedulerJobs[]                @relation("tenant_id_to_institutes")
  scheduler_historys               SchedulerHistory[]             @relation("tenant_id_to_institutes")
  api_logss                        ApiLogs[]                      @relation("tenant_id_to_institutes")
  system_health_logss              SystemHealthLogs[]             @relation("tenant_id_to_institutes")
  error_logss                      ErrorLogs[]                    @relation("tenant_id_to_institutes")
  system_configurationss           SystemConfigurations[]         @relation("tenant_id_to_institutes")
  platform_eventss                 PlatformEvents[]               @relation("tenant_id_to_institutes")
  platform_audit_logss             PlatformAuditLogs[]            @relation("tenant_id_to_institutes")

  @@unique([code])
  @@unique([slug])
  @@unique([email])
}

model Branches {
  id          String     @id @default(uuid())
  tenantId    String
  code        String
  slug        String
  name        String
  displayName String
  email       String
  phone       String
  branchType  BranchType
  status      String     @default("ACTIVE")
  timezone    String     @default("Asia/Kolkata")
  createdAt   DateTime   @default(now())
  createdBy   String
  updatedAt   DateTime   @default(now())
  updatedBy   String
  deletedAt   DateTime
  deletedBy   String
  version     Int        @default(1)
  tenant      Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([slug])
  @@unique([email])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
  @@unique([tenantId, slug])
}

model AcademicYears {
  id           String     @id @default(uuid())
  tenantId     String
  code         String
  name         String
  description  String
  startDate    DateTime
  endDate      DateTime
  displayOrder Int        @default(1)
  isCurrent    Boolean    @default(false)
  isActive     Boolean    @default(true)
  createdAt    DateTime   @default(now())
  createdBy    String
  updatedAt    DateTime   @default(now())
  updatedBy    String
  deletedAt    DateTime
  deletedBy    String
  version      Int        @default(1)
  tenant       Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model Departments {
  id           String     @id @default(uuid())
  tenantId     String
  code         String
  name         String
  description  String
  displayOrder Int        @default(1)
  isActive     Boolean    @default(true)
  isSystem     Boolean    @default(false)
  createdAt    DateTime   @default(now())
  createdBy    String
  updatedAt    DateTime   @default(now())
  updatedBy    String
  deletedAt    DateTime
  deletedBy    String
  version      Int        @default(1)
  tenant       Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model BranchDepartments {
  id           String     @id @default(uuid())
  tenantId     String
  branchId     String
  departmentId String
  displayOrder Int        @default(1)
  isActive     Boolean    @default(true)
  createdAt    DateTime   @default(now())
  createdBy    String
  updatedAt    DateTime   @default(now())
  updatedBy    String
  deletedAt    DateTime
  deletedBy    String
  version      Int        @default(1)
  tenant       Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
  @@unique([tenantId, branchId, departmentId])
}

model Designations {
  id           String     @id @default(uuid())
  tenantId     String
  code         String
  name         String
  description  String
  displayOrder Int        @default(1)
  isActive     Boolean    @default(true)
  isSystem     Boolean    @default(false)
  createdAt    DateTime   @default(now())
  createdBy    String
  updatedAt    DateTime   @default(now())
  updatedBy    String
  deletedAt    DateTime
  deletedBy    String
  version      Int        @default(1)
  tenant       Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model Courses {
  id             String     @id @default(uuid())
  tenantId       String
  code           String
  name           String
  displayName    String
  description    String
  courseType     String     @default("REGULAR")
  durationMonths Int        @default(12)
  displayOrder   Int        @default(1)
  isActive       Boolean    @default(true)
  isSystem       Boolean    @default(false)
  createdAt      DateTime   @default(now())
  createdBy      String
  updatedAt      DateTime   @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int        @default(1)
  tenant         Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model Subjects {
  id           String     @id @default(uuid())
  tenantId     String
  code         String
  name         String
  shortName    String
  displayName  String
  description  String
  subjectType  String     @default("CORE")
  displayOrder Int        @default(1)
  isActive     Boolean    @default(true)
  isSystem     Boolean    @default(false)
  createdAt    DateTime   @default(now())
  createdBy    String
  updatedAt    DateTime   @default(now())
  updatedBy    String
  deletedAt    DateTime
  deletedBy    String
  version      Int        @default(1)
  tenant       Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model CourseSubjects {
  id           String     @id @default(uuid())
  tenantId     String
  courseId     String
  subjectId    String
  displayOrder Int        @default(1)
  isMandatory  Boolean    @default(true)
  totalMarks   Int        @default(100)
  passingMarks Int        @default(40)
  credits      Int        @default(0)
  plannedHours Int        @default(100)
  isActive     Boolean    @default(true)
  createdAt    DateTime   @default(now())
  createdBy    String
  updatedAt    DateTime   @default(now())
  updatedBy    String
  deletedAt    DateTime
  deletedBy    String
  version      Int        @default(1)
  tenant       Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model Chapters {
  id                String     @id @default(uuid())
  tenantId          String
  courseSubjectId   String
  code              String
  name              String
  shortName         String
  description       String
  plannedHours      Int        @default(10)
  estimatedSessions Int        @default(8)
  displayOrder      Int        @default(1)
  isActive          Boolean    @default(true)
  isSystem          Boolean    @default(false)
  createdAt         DateTime   @default(now())
  createdBy         String
  updatedAt         DateTime   @default(now())
  updatedBy         String
  deletedAt         DateTime
  deletedBy         String
  version           Int        @default(1)
  tenant            Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model Topics {
  id                 String     @id @default(uuid())
  tenantId           String
  chapterId          String
  code               String
  name               String
  shortName          String
  description        String
  learningObjectives String
  difficultyLevel    String     @default("MEDIUM")
  plannedHours       Int        @default(4)
  plannedSessions    Int        @default(3)
  displayOrder       Int        @default(1)
  isActive           Boolean    @default(true)
  isSystem           Boolean    @default(false)
  createdAt          DateTime   @default(now())
  createdBy          String
  updatedAt          DateTime   @default(now())
  updatedBy          String
  deletedAt          DateTime
  deletedBy          String
  version            Int        @default(1)
  tenant             Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model BatchDeliveryTypes {
  id                 String             @id @default(uuid())
  tenantId           String
  code               String
  name               String
  description        String
  attendanceMode     AttendanceModeType
  defaultMaxStudents Int                @default(40)
  defaultStartTime   DateTime
  defaultEndTime     DateTime
  colorCode          String
  iconName           String
  displayOrder       Int                @default(1)
  isDefault          Boolean            @default(false)
  isActive           Boolean            @default(true)
  isSystem           Boolean            @default(false)
  createdAt          DateTime           @default(now())
  createdBy          String
  updatedAt          DateTime           @default(now())
  updatedBy          String
  deletedAt          DateTime
  deletedBy          String
  version            Int                @default(1)
  tenant             Institutes         @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model Batches {
  id                 String          @id @default(uuid())
  tenantId           String
  branchId           String
  courseId           String
  academicYearId     String
  deliveryTypeId     String
  code               String
  name               String
  description        String
  status             BatchStatusType
  maxStudents        Int             @default(40)
  startDate          DateTime
  endDate            DateTime
  allowNewAdmissions Boolean         @default(true)
  isActive           Boolean         @default(true)
  isSystem           Boolean         @default(false)
  createdAt          DateTime        @default(now())
  createdBy          String
  updatedAt          DateTime        @default(now())
  updatedBy          String
  deletedAt          DateTime
  deletedBy          String
  version            Int             @default(1)
  tenant             Institutes      @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model Users {
  id                                   String                         @id @default(uuid())
  tenantId                             String
  branchId                             String
  email                                String
  firstName                            String
  lastName                             String
  userType                             UserTypeEnum
  status                               UserStatusType
  isSuperAdmin                         Boolean                        @default(false)
  passwordHash                         String
  forcePasswordChange                  Boolean                        @default(false)
  failedAttempts                       Int                            @default(0)
  lockedUntil                          DateTime?
  lastLoginAt                          DateTime?
  passwordChangedAt                    DateTime?
  createdAt                            DateTime                       @default(now())
  createdBy                            String
  updatedAt                            DateTime                       @default(now())
  updatedBy                            String
  deletedAt                            DateTime
  deletedBy                            String
  version                              Int                            @default(1)
  user_sessionss                       UserSessions[]                 @relation("user_sessions_user_id_to_users")
  auth_eventss                         AuthEvents[]                   @relation("auth_events_user_id_to_users")
  staff_profiless                      StaffProfiles[]                @relation("staff_profiles_user_id_to_users")
  staff_profiles_created_by            StaffProfiles[]                @relation("staff_profiles_created_by_to_users")
  staff_profiles_updated_by            StaffProfiles[]                @relation("staff_profiles_updated_by_to_users")
  staff_profiles_deleted_by            StaffProfiles[]                @relation("staff_profiles_deleted_by_to_users")
  student_profiless                    StudentProfiles[]              @relation("student_profiles_user_id_to_users")
  student_profiles_created_by          StudentProfiles[]              @relation("student_profiles_created_by_to_users")
  student_profiles_updated_by          StudentProfiles[]              @relation("student_profiles_updated_by_to_users")
  student_profiles_deleted_by          StudentProfiles[]              @relation("student_profiles_deleted_by_to_users")
  parent_profiless                     ParentProfiles[]               @relation("parent_profiles_user_id_to_users")
  parent_profiles_created_by           ParentProfiles[]               @relation("parent_profiles_created_by_to_users")
  parent_profiles_updated_by           ParentProfiles[]               @relation("parent_profiles_updated_by_to_users")
  parent_profiles_deleted_by           ParentProfiles[]               @relation("parent_profiles_deleted_by_to_users")
  student_parentss                     StudentParents[]               @relation("student_parents_created_by_to_users")
  student_admissionss                  StudentAdmissions[]            @relation("student_admissions_created_by_to_users")
  student_admissions_updated_by        StudentAdmissions[]            @relation("student_admissions_updated_by_to_users")
  student_admissions_deleted_by        StudentAdmissions[]            @relation("student_admissions_deleted_by_to_users")
  student_batch_enrollmentss           StudentBatchEnrollments[]      @relation("student_batch_enrollments_created_by_to_users")
  student_batch_enrollments_updated_by StudentBatchEnrollments[]      @relation("student_batch_enrollments_updated_by_to_users")
  student_batch_enrollments_deleted_by StudentBatchEnrollments[]      @relation("student_batch_enrollments_deleted_by_to_users")
  staff_departmentss                   StaffDepartments[]             @relation("staff_departments_created_by_to_users")
  staff_departments_updated_by         StaffDepartments[]             @relation("staff_departments_updated_by_to_users")
  staff_departments_deleted_by         StaffDepartments[]             @relation("staff_departments_deleted_by_to_users")
  staff_subjectss                      StaffSubjects[]                @relation("staff_subjects_created_by_to_users")
  staff_subjects_updated_by            StaffSubjects[]                @relation("staff_subjects_updated_by_to_users")
  staff_subjects_deleted_by            StaffSubjects[]                @relation("staff_subjects_deleted_by_to_users")
  student_documentss                   StudentDocuments[]             @relation("student_documents_verified_by_to_users")
  student_documents_created_by         StudentDocuments[]             @relation("student_documents_created_by_to_users")
  student_documents_updated_by         StudentDocuments[]             @relation("student_documents_updated_by_to_users")
  student_documents_deleted_by         StudentDocuments[]             @relation("student_documents_deleted_by_to_users")
  staff_batch_assignmentss             StaffBatchAssignments[]        @relation("staff_batch_assignments_created_by_to_users")
  staff_batch_assignments_updated_by   StaffBatchAssignments[]        @relation("staff_batch_assignments_updated_by_to_users")
  staff_batch_assignments_deleted_by   StaffBatchAssignments[]        @relation("staff_batch_assignments_deleted_by_to_users")
  student_medical_profiless            StudentMedicalProfiles[]       @relation("student_medical_profiles_created_by_to_users")
  student_medical_profiles_updated_by  StudentMedicalProfiles[]       @relation("student_medical_profiles_updated_by_to_users")
  student_medical_profiles_deleted_by  StudentMedicalProfiles[]       @relation("student_medical_profiles_deleted_by_to_users")
  staff_qualificationss                StaffQualifications[]          @relation("staff_qualifications_created_by_to_users")
  staff_qualifications_updated_by      StaffQualifications[]          @relation("staff_qualifications_updated_by_to_users")
  staff_qualifications_deleted_by      StaffQualifications[]          @relation("staff_qualifications_deleted_by_to_users")
  emergency_contactss                  EmergencyContacts[]            @relation("emergency_contacts_created_by_to_users")
  emergency_contacts_updated_by        EmergencyContacts[]            @relation("emergency_contacts_updated_by_to_users")
  emergency_contacts_deleted_by        EmergencyContacts[]            @relation("emergency_contacts_deleted_by_to_users")
  staff_employment_historys            StaffEmploymentHistory[]       @relation("staff_employment_history_created_by_to_users")
  student_status_historys              StudentStatusHistory[]         @relation("student_status_history_changed_by_to_users")
  student_status_history_created_by    StudentStatusHistory[]         @relation("student_status_history_created_by_to_users")
  student_status_history_updated_by    StudentStatusHistory[]         @relation("student_status_history_updated_by_to_users")
  student_status_history_deleted_by    StudentStatusHistory[]         @relation("student_status_history_deleted_by_to_users")
  person_identifierss                  PersonIdentifiers[]            @relation("person_identifiers_created_by_to_users")
  person_identifiers_updated_by        PersonIdentifiers[]            @relation("person_identifiers_updated_by_to_users")
  person_identifiers_deleted_by        PersonIdentifiers[]            @relation("person_identifiers_deleted_by_to_users")
  attendance_sessionss                 AttendanceSessions[]           @relation("attendance_sessions_published_by_to_users")
  attendance_sessions_locked_by        AttendanceSessions[]           @relation("attendance_sessions_locked_by_to_users")
  attendance_sessions_created_by       AttendanceSessions[]           @relation("attendance_sessions_created_by_to_users")
  attendance_sessions_updated_by       AttendanceSessions[]           @relation("attendance_sessions_updated_by_to_users")
  attendance_sessions_deleted_by       AttendanceSessions[]           @relation("attendance_sessions_deleted_by_to_users")
  attendance_recordss                  AttendanceRecords[]            @relation("attendance_records_marked_by_to_users")
  attendance_records_created_by        AttendanceRecords[]            @relation("attendance_records_created_by_to_users")
  attendance_records_updated_by        AttendanceRecords[]            @relation("attendance_records_updated_by_to_users")
  attendance_records_deleted_by        AttendanceRecords[]            @relation("attendance_records_deleted_by_to_users")
  leave_requestss                      LeaveRequests[]                @relation("leave_requests_requester_id_to_users")
  leave_requests_approved_by           LeaveRequests[]                @relation("leave_requests_approved_by_to_users")
  leave_requests_cancelled_by          LeaveRequests[]                @relation("leave_requests_cancelled_by_to_users")
  leave_requests_created_by            LeaveRequests[]                @relation("leave_requests_created_by_to_users")
  leave_requests_updated_by            LeaveRequests[]                @relation("leave_requests_updated_by_to_users")
  leave_requests_deleted_by            LeaveRequests[]                @relation("leave_requests_deleted_by_to_users")
  attendance_adjustmentss              AttendanceAdjustments[]        @relation("attendance_adjustments_changed_by_to_users")
  attendance_adjustments_created_by    AttendanceAdjustments[]        @relation("attendance_adjustments_created_by_to_users")
  leave_attachmentss                   LeaveAttachments[]             @relation("leave_attachments_verified_by_to_users")
  leave_attachments_created_by         LeaveAttachments[]             @relation("leave_attachments_created_by_to_users")
  leave_attachments_updated_by         LeaveAttachments[]             @relation("leave_attachments_updated_by_to_users")
  leave_attachments_deleted_by         LeaveAttachments[]             @relation("leave_attachments_deleted_by_to_users")
  examss                               Exams[]                        @relation("exams_published_by_to_users")
  exams_results_published_by           Exams[]                        @relation("exams_results_published_by_to_users")
  exams_created_by                     Exams[]                        @relation("exams_created_by_to_users")
  exams_updated_by                     Exams[]                        @relation("exams_updated_by_to_users")
  exams_deleted_by                     Exams[]                        @relation("exams_deleted_by_to_users")
  exam_sectionss                       ExamSections[]                 @relation("exam_sections_created_by_to_users")
  exam_sections_updated_by             ExamSections[]                 @relation("exam_sections_updated_by_to_users")
  exam_sections_deleted_by             ExamSections[]                 @relation("exam_sections_deleted_by_to_users")
  exam_questionss                      ExamQuestions[]                @relation("exam_questions_created_by_to_users")
  exam_questions_updated_by            ExamQuestions[]                @relation("exam_questions_updated_by_to_users")
  exam_questions_deleted_by            ExamQuestions[]                @relation("exam_questions_deleted_by_to_users")
  question_paperss                     QuestionPapers[]               @relation("question_papers_created_by_to_users")
  question_papers_updated_by           QuestionPapers[]               @relation("question_papers_updated_by_to_users")
  question_papers_deleted_by           QuestionPapers[]               @relation("question_papers_deleted_by_to_users")
  question_paper_questionss            QuestionPaperQuestions[]       @relation("question_paper_questions_created_by_to_users")
  question_paper_questions_updated_by  QuestionPaperQuestions[]       @relation("question_paper_questions_updated_by_to_users")
  question_paper_questions_deleted_by  QuestionPaperQuestions[]       @relation("question_paper_questions_deleted_by_to_users")
  exam_registrationss                  ExamRegistrations[]            @relation("exam_registrations_created_by_to_users")
  exam_registrations_updated_by        ExamRegistrations[]            @relation("exam_registrations_updated_by_to_users")
  exam_registrations_deleted_by        ExamRegistrations[]            @relation("exam_registrations_deleted_by_to_users")
  exam_attemptss                       ExamAttempts[]                 @relation("exam_attempts_created_by_to_users")
  exam_attempts_updated_by             ExamAttempts[]                 @relation("exam_attempts_updated_by_to_users")
  exam_attempts_deleted_by             ExamAttempts[]                 @relation("exam_attempts_deleted_by_to_users")
  exam_answerss                        ExamAnswers[]                  @relation("exam_answers_evaluated_by_to_users")
  exam_answers_created_by              ExamAnswers[]                  @relation("exam_answers_created_by_to_users")
  exam_answers_updated_by              ExamAnswers[]                  @relation("exam_answers_updated_by_to_users")
  exam_answers_deleted_by              ExamAnswers[]                  @relation("exam_answers_deleted_by_to_users")
  exam_resultss                        ExamResults[]                  @relation("exam_results_published_by_to_users")
  exam_results_re_evaluated_by         ExamResults[]                  @relation("exam_results_re_evaluated_by_to_users")
  exam_results_created_by              ExamResults[]                  @relation("exam_results_created_by_to_users")
  exam_results_updated_by              ExamResults[]                  @relation("exam_results_updated_by_to_users")
  exam_results_deleted_by              ExamResults[]                  @relation("exam_results_deleted_by_to_users")
  exam_result_historys                 ExamResultHistory[]            @relation("exam_result_history_changed_by_to_users")
  exam_documentss                      ExamDocuments[]                @relation("exam_documents_created_by_to_users")
  exam_documents_updated_by            ExamDocuments[]                @relation("exam_documents_updated_by_to_users")
  exam_documents_deleted_by            ExamDocuments[]                @relation("exam_documents_deleted_by_to_users")
  questionss                           Questions[]                    @relation("questions_approved_by_to_users")
  questions_locked_by                  Questions[]                    @relation("questions_locked_by_to_users")
  questions_created_by                 Questions[]                    @relation("questions_created_by_to_users")
  questions_updated_by                 Questions[]                    @relation("questions_updated_by_to_users")
  questions_deleted_by                 Questions[]                    @relation("questions_deleted_by_to_users")
  question_optionss                    QuestionOptions[]              @relation("question_options_created_by_to_users")
  question_options_updated_by          QuestionOptions[]              @relation("question_options_updated_by_to_users")
  question_options_deleted_by          QuestionOptions[]              @relation("question_options_deleted_by_to_users")
  question_explanationss               QuestionExplanations[]         @relation("question_explanations_verified_by_to_users")
  question_explanations_created_by     QuestionExplanations[]         @relation("question_explanations_created_by_to_users")
  question_explanations_updated_by     QuestionExplanations[]         @relation("question_explanations_updated_by_to_users")
  question_explanations_deleted_by     QuestionExplanations[]         @relation("question_explanations_deleted_by_to_users")
  question_tagss                       QuestionTags[]                 @relation("question_tags_created_by_to_users")
  question_tags_updated_by             QuestionTags[]                 @relation("question_tags_updated_by_to_users")
  question_tags_deleted_by             QuestionTags[]                 @relation("question_tags_deleted_by_to_users")
  question_tag_mappingss               QuestionTagMappings[]          @relation("question_tag_mappings_created_by_to_users")
  question_tag_mappings_updated_by     QuestionTagMappings[]          @relation("question_tag_mappings_updated_by_to_users")
  question_tag_mappings_deleted_by     QuestionTagMappings[]          @relation("question_tag_mappings_deleted_by_to_users")
  question_versionss                   QuestionVersions[]             @relation("question_versions_created_by_to_users")
  question_attachmentss                QuestionAttachments[]          @relation("question_attachments_created_by_to_users")
  question_attachments_updated_by      QuestionAttachments[]          @relation("question_attachments_updated_by_to_users")
  question_attachments_deleted_by      QuestionAttachments[]          @relation("question_attachments_deleted_by_to_users")
  question_reviewss                    QuestionReviews[]              @relation("question_reviews_reviewer_id_to_users")
  question_reviews_created_by          QuestionReviews[]              @relation("question_reviews_created_by_to_users")
  question_reviews_updated_by          QuestionReviews[]              @relation("question_reviews_updated_by_to_users")
  question_reviews_deleted_by          QuestionReviews[]              @relation("question_reviews_deleted_by_to_users")
  question_usages                      QuestionUsage[]                @relation("question_usage_deleted_by_to_users")
  question_import_jobss                QuestionImportJobs[]           @relation("question_import_jobs_created_by_to_users")
  question_import_jobs_updated_by      QuestionImportJobs[]           @relation("question_import_jobs_updated_by_to_users")
  question_import_jobs_deleted_by      QuestionImportJobs[]           @relation("question_import_jobs_deleted_by_to_users")
  learning_materialss                  LearningMaterials[]            @relation("learning_materials_published_by_to_users")
  learning_materials_created_by        LearningMaterials[]            @relation("learning_materials_created_by_to_users")
  learning_materials_updated_by        LearningMaterials[]            @relation("learning_materials_updated_by_to_users")
  learning_materials_deleted_by        LearningMaterials[]            @relation("learning_materials_deleted_by_to_users")
  material_versionss                   MaterialVersions[]             @relation("material_versions_created_by_to_users")
  material_attachmentss                MaterialAttachments[]          @relation("material_attachments_created_by_to_users")
  material_attachments_updated_by      MaterialAttachments[]          @relation("material_attachments_updated_by_to_users")
  material_attachments_deleted_by      MaterialAttachments[]          @relation("material_attachments_deleted_by_to_users")
  learning_pathss                      LearningPaths[]                @relation("learning_paths_created_by_to_users")
  learning_paths_updated_by            LearningPaths[]                @relation("learning_paths_updated_by_to_users")
  learning_paths_deleted_by            LearningPaths[]                @relation("learning_paths_deleted_by_to_users")
  learning_path_materialss             LearningPathMaterials[]        @relation("learning_path_materials_created_by_to_users")
  learning_path_materials_updated_by   LearningPathMaterials[]        @relation("learning_path_materials_updated_by_to_users")
  learning_path_materials_deleted_by   LearningPathMaterials[]        @relation("learning_path_materials_deleted_by_to_users")
  student_learning_progresss           StudentLearningProgress[]      @relation("student_learning_progress_created_by_to_users")
  student_learning_progress_updated_by StudentLearningProgress[]      @relation("student_learning_progress_updated_by_to_users")
  student_learning_progress_deleted_by StudentLearningProgress[]      @relation("student_learning_progress_deleted_by_to_users")
  learning_bookmarkss                  LearningBookmarks[]            @relation("learning_bookmarks_student_id_to_users")
  learning_bookmarks_created_by        LearningBookmarks[]            @relation("learning_bookmarks_created_by_to_users")
  learning_bookmarks_updated_by        LearningBookmarks[]            @relation("learning_bookmarks_updated_by_to_users")
  learning_bookmarks_deleted_by        LearningBookmarks[]            @relation("learning_bookmarks_deleted_by_to_users")
  learning_notess                      LearningNotes[]                @relation("learning_notes_student_id_to_users")
  learning_notes_created_by            LearningNotes[]                @relation("learning_notes_created_by_to_users")
  learning_notes_updated_by            LearningNotes[]                @relation("learning_notes_updated_by_to_users")
  learning_notes_deleted_by            LearningNotes[]                @relation("learning_notes_deleted_by_to_users")
  material_discussionss                MaterialDiscussions[]          @relation("material_discussions_author_id_to_users")
  material_discussions_created_by      MaterialDiscussions[]          @relation("material_discussions_created_by_to_users")
  material_discussions_updated_by      MaterialDiscussions[]          @relation("material_discussions_updated_by_to_users")
  material_discussions_deleted_by      MaterialDiscussions[]          @relation("material_discussions_deleted_by_to_users")
  material_assignmentss                MaterialAssignments[]          @relation("material_assignments_created_by_to_users")
  material_assignments_updated_by      MaterialAssignments[]          @relation("material_assignments_updated_by_to_users")
  material_assignments_deleted_by      MaterialAssignments[]          @relation("material_assignments_deleted_by_to_users")
  assignment_submissionss              AssignmentSubmissions[]        @relation("assignment_submissions_evaluated_by_to_users")
  assignment_submissions_created_by    AssignmentSubmissions[]        @relation("assignment_submissions_created_by_to_users")
  assignment_submissions_updated_by    AssignmentSubmissions[]        @relation("assignment_submissions_updated_by_to_users")
  assignment_submissions_deleted_by    AssignmentSubmissions[]        @relation("assignment_submissions_deleted_by_to_users")
  live_class_attendances               LiveClassAttendance[]          @relation("live_class_attendance_marked_by_to_users")
  live_class_resourcess                LiveClassResources[]           @relation("live_class_resources_uploaded_by_to_users")
  live_class_chat_messagess            LiveClassChatMessages[]        @relation("live_class_chat_messages_sender_id_to_users")
  live_class_whiteboard_snapshotss     LiveClassWhiteboardSnapshots[] @relation("live_class_whiteboard_snapshots_captured_by_to_users")
  live_class_raise_handss              LiveClassRaiseHands[]          @relation("live_class_raise_hands_responded_by_to_users")
  live_class_eventss                   LiveClassEvents[]              @relation("live_class_events_triggered_by_to_users")
  student_fee_assignmentss             StudentFeeAssignments[]        @relation("student_fee_assignments_assigned_by_to_users")
  fee_paymentss                        FeePayments[]                  @relation("fee_payments_received_by_to_users")
  payment_refundss                     PaymentRefunds[]               @relation("payment_refunds_approved_by_to_users")
  student_fee_discountss               StudentFeeDiscounts[]          @relation("student_fee_discounts_approved_by_to_users")
  student_fee_penaltiess               StudentFeePenalties[]          @relation("student_fee_penalties_waived_by_to_users")
  fee_waiverss                         FeeWaivers[]                   @relation("fee_waivers_approved_by_to_users")
  payment_reconciliations              PaymentReconciliation[]        @relation("payment_reconciliation_reconciled_by_to_users")
  fee_audit_logss                      FeeAuditLogs[]                 @relation("fee_audit_logs_triggered_by_to_users")
  fee_adjustmentss                     FeeAdjustments[]               @relation("fee_adjustments_adjusted_by_to_users")
  fee_collection_closuress             FeeCollectionClosures[]        @relation("fee_collection_closures_cashier_id_to_users")
  notification_template_versionss      NotificationTemplateVersions[] @relation("notification_template_versions_approved_by_to_users")
  notification_campaign_targetss       NotificationCampaignTargets[]  @relation("notification_campaign_targets_recipient_user_id_to_users")
  notification_queues                  NotificationQueue[]            @relation("notification_queue_recipient_user_id_to_users")
  notification_preferencess            NotificationPreferences[]      @relation("notification_preferences_user_id_to_users")
  notification_subscriptionss          NotificationSubscriptions[]    @relation("notification_subscriptions_user_id_to_users")
  notification_logss                   NotificationLogs[]             @relation("notification_logs_recipient_user_id_to_users")
  push_devicess                        PushDevices[]                  @relation("push_devices_user_id_to_users")
  announcementss                       Announcements[]                @relation("announcements_created_by_user_to_users")
  announcement_readss                  AnnouncementReads[]            @relation("announcement_reads_user_id_to_users")
  communication_audit_logss            CommunicationAuditLogs[]       @relation("communication_audit_logs_triggered_by_to_users")
  report_executionss                   ReportExecutions[]             @relation("report_executions_generated_by_to_users")
  analytics_audit_logss                AnalyticsAuditLogs[]           @relation("analytics_audit_logs_triggered_by_to_users")
  ai_conversationss                    AiConversations[]              @relation("ai_conversations_user_id_to_users")
  ai_requestss                         AiRequests[]                   @relation("ai_requests_triggered_by_user_to_users")
  ai_feedbacks                         AiFeedback[]                   @relation("ai_feedback_user_id_to_users")
  ai_usage_logss                       AiUsageLogs[]                  @relation("ai_usage_logs_user_id_to_users")
  ai_audit_logss                       AiAuditLogs[]                  @relation("ai_audit_logs_triggered_by_to_users")
  storage_objectss                     StorageObjects[]               @relation("storage_objects_owner_user_id_to_users")
  error_logss                          ErrorLogs[]                    @relation("error_logs_resolved_by_user_to_users")
  platform_eventss                     PlatformEvents[]               @relation("platform_events_triggered_by_user_to_users")
  platform_audit_logss                 PlatformAuditLogs[]            @relation("platform_audit_logs_triggered_by_to_users")
  password_reset_tokens                PasswordResetTokens[]          @relation("password_reset_tokens_user_id_to_users")
  tenant                               Institutes                     @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, email])
  @@unique([tenantId, id])
}

model Roles {
  id                                String               @id @default(uuid())
  tenantId                          String
  name                              String
  code                              String
  roleType                          String               @default("CUSTOM")
  isDefault                         Boolean              @default(false)
  isEditable                        Boolean              @default(true)
  isDeletable                       Boolean              @default(true)
  priority                          Int                  @default(1)
  createdAt                         DateTime             @default(now())
  updatedAt                         DateTime             @default(now())
  createdBy                         String
  updatedBy                         String
  deletedAt                         DateTime
  deletedBy                         String
  version                           Int                  @default(1)
  metadata                          Json
  role_permissionss                 RolePermissions[]    @relation("role_permissions_role_id_to_roles")
  user_roless                       UserRoles[]          @relation("user_roles_role_id_to_roles")
  role_menu_visibilitys             RoleMenuVisibility[] @relation("role_menu_visibility_role_id_to_roles")
  role_inheritances                 RoleInheritance[]    @relation("role_inheritance_role_id_to_roles")
  role_inheritance_inherits_role_id RoleInheritance[]    @relation("role_inheritance_inherits_role_id_to_roles")

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
  @@index([tenantId, priority])
}

model Permissions {
  id                                             String                   @id @default(uuid())
  tenantId                                       String
  permissionGroupId                              String
  permissionKey                                  String
  resource                                       String
  action                                         String
  description                                    String
  isSystem                                       Boolean                  @default(false)
  isDeprecated                                   Boolean                  @default(false)
  scope                                          String                   @default("TENANT")
  createdAt                                      DateTime                 @default(now())
  updatedAt                                      DateTime                 @default(now())
  createdBy                                      String
  updatedBy                                      String
  deletedAt                                      DateTime
  deletedBy                                      String
  version                                        Int                      @default(1)
  metadata                                       Json
  role_permissionss                              RolePermissions[]        @relation("role_permissions_permission_id_to_permissions")
  menu_permissionss                              MenuPermissions[]        @relation("menu_permissions_permission_id_to_permissions")
  role_templatess                                RoleTemplates[]          @relation("role_templates_permission_id_to_permissions")
  permission_dependenciess                       PermissionDependencies[] @relation("permission_dependencies_permission_id_to_permissions")
  permission_dependencies_requires_permission_id PermissionDependencies[] @relation("permission_dependencies_requires_permission_id_to_permissions")
  permissionGrouppermission_grou                 PermissionGroups?        @relation("permissions_permission_group_id_to_permission_groups", fields: [permissionGroupId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@index([tenantId, resource, action])
}

model RolePermissions {
  id                      String                 @id @default(uuid())
  tenantId                String
  roleId                  String
  permissionId            String
  createdAt               DateTime               @default(now())
  updatedAt               DateTime               @default(now())
  createdBy               String
  updatedBy               String
  deletedAt               DateTime
  deletedBy               String
  version                 Int                    @default(1)
  metadata                Json
  permission_conditionss  PermissionConditions[] @relation("permission_conditions_role_permission_id_to_role_permissions")
  roleIdroles             Roles?                 @relation("role_permissions_role_id_to_roles", fields: [roleId], references: [id], onDelete: SetNull)
  permissionIdpermissions Permissions?           @relation("role_permissions_permission_id_to_permissions", fields: [permissionId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@index([tenantId, roleId])
}

model UserRoles {
  id               String   @id @default(uuid())
  tenantId         String
  userId           String
  roleId           String
  effectiveFrom    DateTime @default(now())
  effectiveTo      DateTime
  assignedBy       String
  assignmentReason String
  revokedBy        String
  revokedReason    String
  createdAt        DateTime @default(now())
  updatedAt        DateTime @default(now())
  createdBy        String
  updatedBy        String
  deletedAt        DateTime
  deletedBy        String
  version          Int      @default(1)
  metadata         Json
  roleIdroles      Roles?   @relation("user_roles_role_id_to_roles", fields: [roleId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@index([tenantId, userId, effectiveFrom, effectiveTo])
}

model Menus {
  id            String       @id @default(uuid())
  parentId      String
  code          String
  name          String
  route         String
  iconKey       String
  pageTitle     String
  menuType      MenuNodeType
  displayOrder  Int          @default(1)
  showInSidebar Boolean      @default(true)
  isVisible     Boolean      @default(true)
  isSystem      Boolean      @default(true)
  isActive      Boolean      @default(true)
  featureFlag   String
  createdAt     DateTime     @default(now())
  createdBy     String
  updatedAt     DateTime     @default(now())
  updatedBy     String
  deletedAt     DateTime
  deletedBy     String
  version       Int          @default(1)
  menuss        Menus[]      @relation("menus_parent_id_to_menus")
  parentIdmenus Menus?       @relation("menus_parent_id_to_menus", fields: [parentId], references: [id], onDelete: SetNull)

  @@unique([code])
}

model MenuPermissions {
  id                      String       @id @default(uuid())
  tenantId                String
  menuId                  String
  permissionId            String
  createdAt               DateTime     @default(now())
  updatedAt               DateTime     @default(now())
  createdBy               String
  updatedBy               String
  deletedAt               DateTime
  deletedBy               String
  version                 Int          @default(1)
  metadata                Json
  menuIdmenu_master       MenuMaster?  @relation("menu_permissions_menu_id_to_menu_master", fields: [menuId], references: [id], onDelete: SetNull)
  permissionIdpermissions Permissions? @relation("menu_permissions_permission_id_to_permissions", fields: [permissionId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@index([tenantId, menuId])
}

model UserSessions {
  id                String            @id @default(uuid())
  userId            String
  tenantId          String?
  platformSessionId String
  deviceFingerprint String
  deviceName        String
  ipAddress         String
  deviceType        String
  browserName       String
  osName            String
  rawUserAgent      String
  status            SessionStatusType
  refreshTokenHash  String
  isRevoked         Boolean           @default(false)
  lastActiveAt      DateTime          @default(now())
  expiresAt         DateTime
  revokedAt         DateTime?
  revokedReason     String?
  loggedOutAt       DateTime?
  createdAt         DateTime          @default(now())
  auth_eventss      AuthEvents[]      @relation("auth_events_session_id_to_user_sessions")
  userIdusers       Users?            @relation("user_sessions_user_id_to_users", fields: [userId], references: [id], onDelete: SetNull)
}

model AuthEvents {
  id                     String        @id @default(uuid())
  tenantId               String
  userId                 String
  sessionId              String
  email                  String
  eventType              AuthEventType
  ipAddress              String
  rawUserAgent           String
  locationCountry        String
  locationRegion         String
  locationTimezone       String
  details                Json
  createdAt              DateTime      @default(now())
  userIdusers            Users?        @relation("auth_events_user_id_to_users", fields: [userId], references: [id], onDelete: SetNull)
  sessionIduser_sessions UserSessions? @relation("auth_events_session_id_to_user_sessions", fields: [sessionId], references: [id], onDelete: SetNull)

  @@index([userId, createdAt])
  @@index([eventType, createdAt])
}

model PasswordResetTokens {
  id          String    @id @default(uuid())
  tenantId    String
  userId      String
  email       String
  hashedToken String
  expiresAt   DateTime
  usedAt      DateTime?
  createdAt   DateTime  @default(now())
  userIdusers Users?    @relation("password_reset_tokens_user_id_to_users", fields: [userId], references: [id], onDelete: Cascade)

  @@index([hashedToken])
  @@index([userId, expiresAt])
}

model StaffProfiles {
  userId                    String                   @id @default(uuid())
  tenantId                  String
  employeeCode              String
  designationId             String
  employmentType            EmploymentTypeEnum
  employmentStatus          EmploymentStatusEnum
  joinedAt                  DateTime
  resignedAt                DateTime
  officialEmail             String
  workPhone                 String
  createdAt                 DateTime                 @default(now())
  createdBy                 String
  updatedAt                 DateTime                 @default(now())
  updatedBy                 String
  deletedAt                 DateTime
  deletedBy                 String
  version                   Int                      @default(1)
  staff_departmentss        StaffDepartments[]       @relation("staff_departments_staff_profile_id_to_staff_profiles")
  staff_subjectss           StaffSubjects[]          @relation("staff_subjects_staff_profile_id_to_staff_profiles")
  staff_batch_assignmentss  StaffBatchAssignments[]  @relation("staff_batch_assignments_staff_profile_id_to_staff_profiles")
  staff_qualificationss     StaffQualifications[]    @relation("staff_qualifications_staff_profile_id_to_staff_profiles")
  staff_employment_historys StaffEmploymentHistory[] @relation("staff_employment_history_staff_profile_id_to_staff_profiles")
  attendance_sessionss      AttendanceSessions[]     @relation("attendance_sessions_staff_profile_id_to_staff_profiles")
  live_class_instructorss   LiveClassInstructors[]   @relation("live_class_instructors_staff_profile_id_to_staff_profiles")
  userIdusers               Users?                   @relation("staff_profiles_user_id_to_users", fields: [userId], references: [id], onDelete: SetNull)
  tenant                    Institutes               @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers            Users?                   @relation("staff_profiles_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers            Users?                   @relation("staff_profiles_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers            Users?                   @relation("staff_profiles_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
}

model StudentProfiles {
  userId                      String                   @id @default(uuid())
  tenantId                    String
  studentCode                 String
  admittedAt                  DateTime                 @default(now())
  dateOfBirth                 DateTime
  gender                      GenderType
  bloodGroup                  BloodGroupType
  academicStatus              AcademicStatusEnum
  createdAt                   DateTime                 @default(now())
  createdBy                   String
  updatedAt                   DateTime                 @default(now())
  updatedBy                   String
  deletedAt                   DateTime
  deletedBy                   String
  version                     Int                      @default(1)
  profileVersion              Int                      @default(1)
  lastProfileUpdatedAt        DateTime                 @default(now())
  profileCompletionPercentage Decimal                  @default(0.00)
  student_parentss            StudentParents[]         @relation("student_parents_student_profile_id_to_student_profiles")
  student_admissionss         StudentAdmissions[]      @relation("student_admissions_student_profile_id_to_student_profiles")
  student_documentss          StudentDocuments[]       @relation("student_documents_student_profile_id_to_student_profiles")
  student_medical_profiless   StudentMedicalProfiles[] @relation("student_medical_profiles_student_profile_id_to_student_profiles")
  emergency_contactss         EmergencyContacts[]      @relation("emergency_contacts_student_profile_id_to_student_profiles")
  student_status_historys     StudentStatusHistory[]   @relation("student_status_history_student_profile_id_to_student_profiles")
  person_identifierss         PersonIdentifiers[]      @relation("person_identifiers_student_profile_id_to_student_profiles")
  userIdusers                 Users?                   @relation("student_profiles_user_id_to_users", fields: [userId], references: [id], onDelete: SetNull)
  tenant                      Institutes               @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers              Users?                   @relation("student_profiles_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers              Users?                   @relation("student_profiles_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers              Users?                   @relation("student_profiles_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
}

model ParentProfiles {
  userId           String           @id @default(uuid())
  tenantId         String
  occupation       String
  educationLevel   String
  createdAt        DateTime         @default(now())
  createdBy        String
  updatedAt        DateTime         @default(now())
  updatedBy        String
  deletedAt        DateTime
  deletedBy        String
  version          Int              @default(1)
  student_parentss StudentParents[] @relation("student_parents_parent_profile_id_to_parent_profiles")
  userIdusers      Users?           @relation("parent_profiles_user_id_to_users", fields: [userId], references: [id], onDelete: SetNull)
  tenant           Institutes       @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers   Users?           @relation("parent_profiles_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers   Users?           @relation("parent_profiles_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers   Users?           @relation("parent_profiles_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
}

model StudentParents {
  studentProfileId               String
  parentProfileId                String
  tenantId                       String
  relationshipType               ParentRelationshipTypeEnum
  isPrimaryGuardian              Boolean                    @default(false)
  createdAt                      DateTime                   @default(now())
  createdBy                      String
  studentProfileIstudent_profile StudentProfiles?           @relation("student_parents_student_profile_id_to_student_profiles", fields: [studentProfileId], references: [userId], onDelete: SetNull)
  parentProfileIdparent_profiles ParentProfiles?            @relation("student_parents_parent_profile_id_to_parent_profiles", fields: [parentProfileId], references: [userId], onDelete: SetNull)
  tenant                         Institutes                 @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers                 Users?                     @relation("student_parents_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)

  @@id([studentProfileId, parentProfileId])
}

model StudentAdmissions {
  id                             String              @id @default(uuid())
  tenantId                       String
  studentProfileId               String
  admissionNumber                String
  academicYearId                 String
  courseId                       String
  branchId                       String
  feeStructureId                 String
  admissionDate                  DateTime            @default(now())
  admissionStatus                AdmissionStatusEnum
  remarks                        String
  deletedAt                      DateTime
  deletedBy                      String
  createdAt                      DateTime            @default(now())
  createdBy                      String
  updatedAt                      DateTime            @default(now())
  updatedBy                      String
  version                        Int                 @default(1)
  tenant                         Institutes          @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  studentProfileIstudent_profile StudentProfiles?    @relation("student_admissions_student_profile_id_to_student_profiles", fields: [studentProfileId], references: [userId], onDelete: SetNull)
  deletedByusers                 Users?              @relation("student_admissions_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
  createdByusers                 Users?              @relation("student_admissions_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers                 Users?              @relation("student_admissions_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([tenantId, admissionNumber])
}

model StudentBatchEnrollments {
  id                 String          @id @default(uuid())
  studentAdmissionId String
  batchId            String
  tenantId           String
  joinedAt           DateTime        @default(now())
  leftAt             DateTime
  status             BatchStatusType
  isPrimary          Boolean         @default(true)
  createdAt          DateTime        @default(now())
  createdBy          String
  updatedAt          DateTime        @default(now())
  updatedBy          String
  deletedAt          DateTime
  deletedBy          String
  version            Int             @default(1)
  tenant             Institutes      @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers     Users?          @relation("student_batch_enrollments_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers     Users?          @relation("student_batch_enrollments_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers     Users?          @relation("student_batch_enrollments_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model StaffDepartments {
  staffProfileId               String
  branchId                     String
  departmentId                 String
  tenantId                     String
  isPrimary                    Boolean        @default(true)
  isActive                     Boolean        @default(true)
  createdAt                    DateTime       @default(now())
  createdBy                    String
  updatedAt                    DateTime       @default(now())
  updatedBy                    String
  deletedAt                    DateTime
  deletedBy                    String
  version                      Int            @default(1)
  staffProfileIdstaff_profiles StaffProfiles? @relation("staff_departments_staff_profile_id_to_staff_profiles", fields: [staffProfileId], references: [userId], onDelete: SetNull)
  tenant                       Institutes     @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers               Users?         @relation("staff_departments_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers               Users?         @relation("staff_departments_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers               Users?         @relation("staff_departments_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@id([staffProfileId, branchId, departmentId])
  @@unique([tenantId, branchId, departmentId])
}

model StaffSubjects {
  staffProfileId               String
  subjectId                    String
  tenantId                     String
  isActive                     Boolean        @default(true)
  createdAt                    DateTime       @default(now())
  createdBy                    String
  updatedAt                    DateTime       @default(now())
  updatedBy                    String
  deletedAt                    DateTime
  deletedBy                    String
  version                      Int            @default(1)
  staffProfileIdstaff_profiles StaffProfiles? @relation("staff_subjects_staff_profile_id_to_staff_profiles", fields: [staffProfileId], references: [userId], onDelete: SetNull)
  tenant                       Institutes     @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers               Users?         @relation("staff_subjects_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers               Users?         @relation("staff_subjects_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers               Users?         @relation("staff_subjects_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@id([staffProfileId, subjectId])
}

model StudentDocuments {
  id                             String           @id @default(uuid())
  studentProfileId               String
  tenantId                       String
  documentTypeId                 String
  storageKey                     String
  status                         String           @default("PENDING")
  verifiedBy                     String
  verifiedAt                     DateTime
  expiryDate                     DateTime
  createdAt                      DateTime         @default(now())
  createdBy                      String
  updatedAt                      DateTime         @default(now())
  updatedBy                      String
  deletedAt                      DateTime
  deletedBy                      String
  version                        Int              @default(1)
  studentProfileIstudent_profile StudentProfiles? @relation("student_documents_student_profile_id_to_student_profiles", fields: [studentProfileId], references: [userId], onDelete: SetNull)
  tenant                         Institutes       @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  verifiedByusers                Users?           @relation("student_documents_verified_by_to_users", fields: [verifiedBy], references: [id], onDelete: SetNull)
  createdByusers                 Users?           @relation("student_documents_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers                 Users?           @relation("student_documents_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers                 Users?           @relation("student_documents_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model StaffBatchAssignments {
  id                           String         @id @default(uuid())
  staffProfileId               String
  batchId                      String
  subjectId                    String
  tenantId                     String
  effectiveFrom                DateTime       @default(now())
  effectiveTo                  DateTime
  isActive                     Boolean        @default(true)
  createdAt                    DateTime       @default(now())
  createdBy                    String
  updatedAt                    DateTime       @default(now())
  updatedBy                    String
  deletedAt                    DateTime
  deletedBy                    String
  version                      Int            @default(1)
  staffProfileIdstaff_profiles StaffProfiles? @relation("staff_batch_assignments_staff_profile_id_to_staff_profiles", fields: [staffProfileId], references: [userId], onDelete: SetNull)
  tenant                       Institutes     @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers               Users?         @relation("staff_batch_assignments_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers               Users?         @relation("staff_batch_assignments_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers               Users?         @relation("staff_batch_assignments_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model StudentMedicalProfiles {
  studentProfileId               String           @id @default(uuid())
  tenantId                       String
  bloodGroup                     String
  allergies                      String
  medicalConditions              String
  emergencyNotes                 String
  expiryDate                     DateTime
  createdAt                      DateTime         @default(now())
  createdBy                      String
  updatedAt                      DateTime         @default(now())
  updatedBy                      String
  deletedAt                      DateTime
  deletedBy                      String
  version                        Int              @default(1)
  studentProfileIstudent_profile StudentProfiles? @relation("student_medical_profiles_student_profile_id_to_student_profiles", fields: [studentProfileId], references: [userId], onDelete: SetNull)
  tenant                         Institutes       @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers                 Users?           @relation("student_medical_profiles_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers                 Users?           @relation("student_medical_profiles_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers                 Users?           @relation("student_medical_profiles_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
}

model StaffQualifications {
  id                           String         @id @default(uuid())
  staffProfileId               String
  tenantId                     String
  degree                       String
  institution                  String
  yearCompleted                Int
  experienceMonths             Int            @default(0)
  certificatesMetadata         Json
  createdAt                    DateTime       @default(now())
  createdBy                    String
  updatedAt                    DateTime       @default(now())
  updatedBy                    String
  deletedAt                    DateTime
  deletedBy                    String
  version                      Int            @default(1)
  staffProfileIdstaff_profiles StaffProfiles? @relation("staff_qualifications_staff_profile_id_to_staff_profiles", fields: [staffProfileId], references: [userId], onDelete: SetNull)
  tenant                       Institutes     @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers               Users?         @relation("staff_qualifications_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers               Users?         @relation("staff_qualifications_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers               Users?         @relation("staff_qualifications_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model EmergencyContacts {
  id                             String           @id @default(uuid())
  studentProfileId               String
  tenantId                       String
  name                           String
  relationship                   String
  phone                          String
  email                          String
  isPrimary                      Boolean          @default(false)
  createdAt                      DateTime         @default(now())
  createdBy                      String
  updatedAt                      DateTime         @default(now())
  updatedBy                      String
  deletedAt                      DateTime
  deletedBy                      String
  version                        Int              @default(1)
  studentProfileIstudent_profile StudentProfiles? @relation("emergency_contacts_student_profile_id_to_student_profiles", fields: [studentProfileId], references: [userId], onDelete: SetNull)
  tenant                         Institutes       @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers                 Users?           @relation("emergency_contacts_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers                 Users?           @relation("emergency_contacts_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers                 Users?           @relation("emergency_contacts_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([email])
  @@unique([tenantId, id])
}

model StaffEmploymentHistory {
  id                           String               @id @default(uuid())
  staffProfileId               String
  tenantId                     String
  designationId                String
  departmentId                 String
  employmentStatus             EmploymentStatusEnum
  eventReason                  String
  effectiveDate                DateTime             @default(now())
  createdAt                    DateTime             @default(now())
  createdBy                    String
  staffProfileIdstaff_profiles StaffProfiles?       @relation("staff_employment_history_staff_profile_id_to_staff_profiles", fields: [staffProfileId], references: [userId], onDelete: SetNull)
  tenant                       Institutes           @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers               Users?               @relation("staff_employment_history_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model StudentStatusHistory {
  id                             String           @id @default(uuid())
  studentProfileId               String
  tenantId                       String
  fromStatus                     String
  toStatus                       String
  changedAt                      DateTime         @default(now())
  changedBy                      String
  reason                         String
  createdAt                      DateTime         @default(now())
  createdBy                      String
  updatedAt                      DateTime         @default(now())
  updatedBy                      String
  deletedAt                      DateTime
  deletedBy                      String
  version                        Int              @default(1)
  studentProfileIstudent_profile StudentProfiles? @relation("student_status_history_student_profile_id_to_student_profiles", fields: [studentProfileId], references: [userId], onDelete: SetNull)
  tenant                         Institutes       @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  changedByusers                 Users?           @relation("student_status_history_changed_by_to_users", fields: [changedBy], references: [id], onDelete: SetNull)
  createdByusers                 Users?           @relation("student_status_history_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers                 Users?           @relation("student_status_history_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers                 Users?           @relation("student_status_history_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model PersonIdentifiers {
  id                             String           @id @default(uuid())
  studentProfileId               String
  tenantId                       String
  identifierType                 String
  identifierValue                String
  isActive                       Boolean          @default(true)
  createdAt                      DateTime         @default(now())
  createdBy                      String
  updatedAt                      DateTime         @default(now())
  updatedBy                      String
  deletedAt                      DateTime
  deletedBy                      String
  version                        Int              @default(1)
  studentProfileIstudent_profile StudentProfiles? @relation("person_identifiers_student_profile_id_to_student_profiles", fields: [studentProfileId], references: [userId], onDelete: SetNull)
  tenant                         Institutes       @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers                 Users?           @relation("person_identifiers_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers                 Users?           @relation("person_identifiers_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers                 Users?           @relation("person_identifiers_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model AttendanceSessions {
  id                           String                      @id @default(uuid())
  tenantId                     String
  branchId                     String
  academicYearId               String
  batchId                      String
  subjectId                    String
  staffProfileId               String
  attendanceDate               DateTime
  startsAt                     DateTime
  endsAt                       DateTime
  sessionStatus                AttendanceSessionStatusEnum
  publishedBy                  String
  publishedAt                  DateTime
  lockedBy                     String
  lockedAt                     DateTime
  remarks                      String
  createdAt                    DateTime                    @default(now())
  createdBy                    String
  updatedAt                    DateTime                    @default(now())
  updatedBy                    String
  deletedAt                    DateTime
  deletedBy                    String
  version                      Int                         @default(1)
  attendance_recordss          AttendanceRecords[]         @relation("attendance_records_attendance_session_id_to_attendance_sessions")
  tenant                       Institutes                  @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  staffProfileIdstaff_profiles StaffProfiles?              @relation("attendance_sessions_staff_profile_id_to_staff_profiles", fields: [staffProfileId], references: [userId], onDelete: SetNull)
  publishedByusers             Users?                      @relation("attendance_sessions_published_by_to_users", fields: [publishedBy], references: [id], onDelete: SetNull)
  lockedByusers                Users?                      @relation("attendance_sessions_locked_by_to_users", fields: [lockedBy], references: [id], onDelete: SetNull)
  createdByusers               Users?                      @relation("attendance_sessions_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers               Users?                      @relation("attendance_sessions_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers               Users?                      @relation("attendance_sessions_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model AttendanceRecords {
  id                             String                  @id @default(uuid())
  attendanceSessionId            String
  studentAdmissionId             String
  tenantId                       String
  attendanceStatus               AttendanceStatusEnum
  lateMinutes                    Int                     @default(0)
  markedBy                       String
  markedAt                       DateTime
  remarks                        String
  createdAt                      DateTime                @default(now())
  createdBy                      String
  updatedAt                      DateTime                @default(now())
  updatedBy                      String
  deletedAt                      DateTime
  deletedBy                      String
  version                        Int                     @default(1)
  attendance_adjustmentss        AttendanceAdjustments[] @relation("attendance_adjustments_attendance_record_id_to_attendance_records")
  attendanceSessiattendance_sess AttendanceSessions?     @relation("attendance_records_attendance_session_id_to_attendance_sessions", fields: [attendanceSessionId], references: [id], onDelete: SetNull)
  tenant                         Institutes              @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  markedByusers                  Users?                  @relation("attendance_records_marked_by_to_users", fields: [markedBy], references: [id], onDelete: SetNull)
  createdByusers                 Users?                  @relation("attendance_records_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers                 Users?                  @relation("attendance_records_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers                 Users?                  @relation("attendance_records_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model LeaveRequests {
  id                 String             @id @default(uuid())
  tenantId           String
  requesterId        String
  requesterType      UserTypeEnum
  leaveCategory      LeaveCategoryEnum
  leaveStatus        LeaveStatusEnum
  startDate          DateTime
  endDate            DateTime
  reason             String
  approvedBy         String
  approvedAt         DateTime
  rejectionReason    String
  cancelledBy        String
  cancelledAt        DateTime
  createdAt          DateTime           @default(now())
  createdBy          String
  updatedAt          DateTime           @default(now())
  updatedBy          String
  deletedAt          DateTime
  deletedBy          String
  version            Int                @default(1)
  leave_attachmentss LeaveAttachments[] @relation("leave_attachments_leave_request_id_to_leave_requests")
  tenant             Institutes         @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  requesterIdusers   Users?             @relation("leave_requests_requester_id_to_users", fields: [requesterId], references: [id], onDelete: SetNull)
  approvedByusers    Users?             @relation("leave_requests_approved_by_to_users", fields: [approvedBy], references: [id], onDelete: SetNull)
  cancelledByusers   Users?             @relation("leave_requests_cancelled_by_to_users", fields: [cancelledBy], references: [id], onDelete: SetNull)
  createdByusers     Users?             @relation("leave_requests_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers     Users?             @relation("leave_requests_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers     Users?             @relation("leave_requests_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model AttendanceAdjustments {
  id                             String               @id @default(uuid())
  attendanceRecordId             String
  tenantId                       String
  oldStatus                      AttendanceStatusEnum
  newStatus                      AttendanceStatusEnum
  reason                         String
  changedBy                      String
  changedAt                      DateTime             @default(now())
  createdAt                      DateTime             @default(now())
  createdBy                      String
  attendanceRecorattendance_reco AttendanceRecords?   @relation("attendance_adjustments_attendance_record_id_to_attendance_records", fields: [attendanceRecordId], references: [id], onDelete: SetNull)
  tenant                         Institutes           @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  changedByusers                 Users?               @relation("attendance_adjustments_changed_by_to_users", fields: [changedBy], references: [id], onDelete: SetNull)
  createdByusers                 Users?               @relation("attendance_adjustments_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model LeaveAttachments {
  id                           String         @id @default(uuid())
  leaveRequestId               String
  tenantId                     String
  documentType                 String
  storageObjectId              String
  bucketName                   String
  mimeType                     String
  fileSizeBytes                BigInt
  isVerified                   Boolean        @default(false)
  verifiedBy                   String
  verifiedAt                   DateTime
  createdAt                    DateTime       @default(now())
  createdBy                    String
  updatedAt                    DateTime       @default(now())
  updatedBy                    String
  deletedAt                    DateTime
  deletedBy                    String
  version                      Int            @default(1)
  leaveRequestIdleave_requests LeaveRequests? @relation("leave_attachments_leave_request_id_to_leave_requests", fields: [leaveRequestId], references: [id], onDelete: SetNull)
  tenant                       Institutes     @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  verifiedByusers              Users?         @relation("leave_attachments_verified_by_to_users", fields: [verifiedBy], references: [id], onDelete: SetNull)
  createdByusers               Users?         @relation("leave_attachments_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers               Users?         @relation("leave_attachments_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers               Users?         @relation("leave_attachments_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model Exams {
  id                      String                @id @default(uuid())
  tenantId                String
  courseId                String
  batchId                 String
  subjectId               String
  academicYearId          String
  questionPaperId         String
  title                   String
  description             String
  examType                ExamTypeEnum
  mode                    ExamModeEnum
  totalMarks              Decimal
  passingMarks            Decimal
  negativeMarkingEnabled  Boolean               @default(false)
  negativeMarkingValue    Decimal
  durationMinutes         Int                   @default(180)
  scheduledStartAt        DateTime
  scheduledEndAt          DateTime
  publishStatus           ExamPublishStatusEnum
  publishedBy             String
  publishedAt             DateTime
  publishedFromIp         String
  publishedDevice         String
  resultsPublishedAt      DateTime
  resultsPublishedBy      String
  instructions            String
  calculatorAllowed       Boolean               @default(false)
  roughSheetAllowed       Boolean               @default(true)
  omrTemplateId           String
  omrSheetCount           Int
  version                 Int                   @default(1)
  publishedVersion        Int
  isLocked                Boolean               @default(false)
  lockedAt                DateTime
  lockedBy                String
  status                  ExamStatusEnum
  createdAt               DateTime              @default(now())
  createdBy               String
  updatedAt               DateTime              @default(now())
  updatedBy               String
  deletedAt               DateTime
  deletedBy               String
  tenant                  Institutes            @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  publishedByusers        Users?                @relation("exams_published_by_to_users", fields: [publishedBy], references: [id], onDelete: SetNull)
  resultsPublishedByusers Users?                @relation("exams_results_published_by_to_users", fields: [resultsPublishedBy], references: [id], onDelete: SetNull)
  createdByusers          Users?                @relation("exams_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers          Users?                @relation("exams_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers          Users?                @relation("exams_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model ExamSections {
  id                       String     @id @default(uuid())
  tenantId                 String
  examId                   String
  name                     String
  displayOrder             Int        @default(1)
  description              String
  totalMarks               Decimal
  questionCount            Int        @default(0)
  marksPerQuestion         Decimal
  negativeMarksPerQuestion Decimal
  durationMinutes          Int
  subjectId                String
  createdAt                DateTime   @default(now())
  createdBy                String
  updatedAt                DateTime   @default(now())
  updatedBy                String
  deletedAt                DateTime
  deletedBy                String
  version                  Int        @default(1)
  tenant                   Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers           Users?     @relation("exam_sections_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers           Users?     @relation("exam_sections_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers           Users?     @relation("exam_sections_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([examId, displayOrder])
}

model ExamQuestions {
  id             String                 @id @default(uuid())
  tenantId       String
  examId         String
  sectionId      String
  questionBankId String
  displayOrder   Int                    @default(1)
  marks          Decimal
  negativeMarks  Decimal
  questionType   String                 @default("MCQ")
  difficulty     QuestionDifficultyEnum
  topicTag       String
  chapterId      String
  createdAt      DateTime               @default(now())
  createdBy      String
  updatedAt      DateTime               @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int                    @default(1)
  tenant         Institutes             @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers Users?                 @relation("exam_questions_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers Users?                 @relation("exam_questions_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers Users?                 @relation("exam_questions_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([examId, displayOrder])
}

model QuestionPapers {
  id               String     @id @default(uuid())
  tenantId         String
  paperCode        String
  title            String
  courseId         String
  subjectId        String
  totalMarks       Decimal
  durationMinutes  Int
  instructions     String
  status           String     @default("DRAFT")
  version          Int        @default(1)
  publishedVersion Int
  deletedAt        DateTime
  deletedBy        String
  createdAt        DateTime   @default(now())
  createdBy        String
  updatedAt        DateTime   @default(now())
  updatedBy        String
  tenant           Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  deletedByusers   Users?     @relation("question_papers_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
  createdByusers   Users?     @relation("question_papers_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers   Users?     @relation("question_papers_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([tenantId, paperCode])
}

model QuestionPaperQuestions {
  id             String   @id @default(uuid())
  tenantId       String
  paperId        String
  questionId     String
  section        String
  marks          Decimal
  negativeMarks  Decimal
  displayOrder   Int      @default(1)
  deletedAt      DateTime
  deletedBy      String
  createdAt      DateTime @default(now())
  createdBy      String
  updatedAt      DateTime @default(now())
  updatedBy      String
  version        Int      @default(1)
  deletedByusers Users?   @relation("question_paper_questions_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
  createdByusers Users?   @relation("question_paper_questions_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers Users?   @relation("question_paper_questions_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([paperId, questionId])
  @@unique([paperId, displayOrder])
  @@unique([questionId, version])
  @@unique([questionId])
}

model ExamRegistrations {
  id                    String                 @id @default(uuid())
  tenantId              String
  examId                String
  studentAdmissionId    String
  registrationNumber    String
  rollNumber            String
  hallTicketNumber      String
  registrationStatus    RegistrationStatusEnum
  isEligible            Boolean                @default(true)
  eligibilityRemarks    String
  feeStatus             FeeStatusEnum
  admitCardIssued       Boolean                @default(false)
  admitCardIssuedAt     DateTime
  admitCardDownloadedAt DateTime
  registeredAt          DateTime               @default(now())
  seatNumber            String
  roomNumber            String
  createdAt             DateTime               @default(now())
  createdBy             String
  updatedAt             DateTime               @default(now())
  updatedBy             String
  deletedAt             DateTime
  deletedBy             String
  version               Int                    @default(1)
  tenant                Institutes             @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers        Users?                 @relation("exam_registrations_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers        Users?                 @relation("exam_registrations_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers        Users?                 @relation("exam_registrations_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([examId, studentAdmissionId])
}

model ExamAttempts {
  id                    String            @id @default(uuid())
  tenantId              String
  examId                String
  studentAdmissionId    String
  startedAt             DateTime
  submittedAt           DateTime
  autoSubmittedAt       DateTime
  lastActivityAt        DateTime
  status                AttemptStatusEnum
  submittedBySystem     Boolean           @default(false)
  deviceType            String
  browserName           String
  browserVersion        String
  osName                String
  ipAddress             String
  country               String
  deviceMetadata        Json
  userAgent             String
  timeTakenSeconds      Int
  timePausedSeconds     Int               @default(0)
  proctoringSessionId   String
  proctoringStatus      String
  omrSheetId            String
  answerSheetReceived   Boolean           @default(false)
  answerSheetReceivedAt DateTime
  createdAt             DateTime          @default(now())
  createdBy             String
  updatedAt             DateTime          @default(now())
  updatedBy             String
  deletedAt             DateTime
  deletedBy             String
  version               Int               @default(1)
  tenant                Institutes        @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers        Users?            @relation("exam_attempts_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers        Users?            @relation("exam_attempts_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers        Users?            @relation("exam_attempts_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([examId, studentAdmissionId])
}

model ExamAnswers {
  id               String               @id @default(uuid())
  tenantId         String
  attemptId        String
  questionId       String
  selectedOption   String
  answerText       String
  isCorrect        Boolean
  marksAwarded     Decimal
  answerStatus     AnswerStatusEnum
  evaluationStatus EvaluationStatusEnum
  evaluatedBy      String
  evaluatedAt      DateTime
  answeredAt       DateTime
  createdAt        DateTime             @default(now())
  createdBy        String
  updatedAt        DateTime             @default(now())
  updatedBy        String
  deletedAt        DateTime
  deletedBy        String
  version          Int                  @default(1)
  tenant           Institutes           @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  evaluatedByusers Users?               @relation("exam_answers_evaluated_by_to_users", fields: [evaluatedBy], references: [id], onDelete: SetNull)
  createdByusers   Users?               @relation("exam_answers_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers   Users?               @relation("exam_answers_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers   Users?               @relation("exam_answers_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([attemptId, questionId])
  @@unique([questionId, version])
  @@unique([questionId])
}

model ExamResults {
  id                    String           @id @default(uuid())
  tenantId              String
  examId                String
  attemptId             String
  studentAdmissionId    String
  resultStatus          ResultStatusEnum
  totalMarks            Decimal
  obtainedMarks         Decimal
  correct               Int              @default(0)
  wrong                 Int              @default(0)
  skipped               Int              @default(0)
  percentage            Decimal
  percentile            Decimal
  rank                  Int
  passingMarks          Decimal
  passFail              Boolean
  grade                 String
  isFinal               Boolean          @default(false)
  reEvaluationRequested Boolean          @default(false)
  reEvaluatedAt         DateTime
  reEvaluatedBy         String
  aiEvaluationMetadata  Json
  resultHash            String
  publishedAt           DateTime
  publishedBy           String
  createdAt             DateTime         @default(now())
  createdBy             String
  updatedAt             DateTime         @default(now())
  updatedBy             String
  deletedAt             DateTime
  deletedBy             String
  version               Int              @default(1)
  tenant                Institutes       @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  reEvaluatedByusers    Users?           @relation("exam_results_re_evaluated_by_to_users", fields: [reEvaluatedBy], references: [id], onDelete: SetNull)
  publishedByusers      Users?           @relation("exam_results_published_by_to_users", fields: [publishedBy], references: [id], onDelete: SetNull)
  createdByusers        Users?           @relation("exam_results_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers        Users?           @relation("exam_results_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers        Users?           @relation("exam_results_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([examId, studentAdmissionId])
  @@unique([examId, attemptId])
}

model ExamResultHistory {
  id                  String     @id @default(uuid())
  tenantId            String
  resultId            String
  examId              String
  studentEnrollmentId String
  oldTotalMarks       Decimal
  oldObtainedMarks    Decimal
  newTotalMarks       Decimal
  newObtainedMarks    Decimal
  reason              String
  changedBy           String
  changedAt           DateTime   @default(now())
  resultSnapshot      Json
  tenant              Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  changedByusers      Users?     @relation("exam_result_history_changed_by_to_users", fields: [changedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model ExamDocuments {
  id               String               @id @default(uuid())
  tenantId         String
  examId           String
  documentCategory DocumentCategoryEnum
  displayName      String
  description      String
  storageObjectId  String
  mimeType         String
  fileSizeBytes    BigInt
  isPublic         Boolean              @default(false)
  isActive         Boolean              @default(true)
  displayOrder     Int                  @default(1)
  createdAt        DateTime             @default(now())
  createdBy        String
  updatedAt        DateTime             @default(now())
  updatedBy        String
  deletedAt        DateTime
  deletedBy        String
  version          Int                  @default(1)
  tenant           Institutes           @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers   Users?               @relation("exam_documents_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers   Users?               @relation("exam_documents_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers   Users?               @relation("exam_documents_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([examId, displayOrder])
}

model Questions {
  id                   String                 @id @default(uuid())
  tenantId             String
  subjectId            String
  chapterId            String
  topicId              String
  questionCode         String
  questionText         String
  questionType         QuestionTypeEnum
  difficulty           QuestionDifficultyEnum
  bloomsLevel          BloomsLevelEnum
  language             String                 @default("EN")
  source               QuestionSourceEnum
  questionStatus       QuestionStatusEnum
  publishedVersion     Int
  isLocked             Boolean                @default(false)
  lockedAt             DateTime
  lockedBy             String
  approvedBy           String
  approvedAt           DateTime
  aiMetadata           Json
  embeddingMetadata    Json
  difficultyPrediction Json
  taxonomyMetadata     Json
  createdAt            DateTime               @default(now())
  createdBy            String
  updatedAt            DateTime               @default(now())
  updatedBy            String
  deletedAt            DateTime
  deletedBy            String
  version              Int                    @default(1)
  tenant               Institutes             @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  lockedByusers        Users?                 @relation("questions_locked_by_to_users", fields: [lockedBy], references: [id], onDelete: SetNull)
  approvedByusers      Users?                 @relation("questions_approved_by_to_users", fields: [approvedBy], references: [id], onDelete: SetNull)
  createdByusers       Users?                 @relation("questions_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers       Users?                 @relation("questions_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers       Users?                 @relation("questions_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model QuestionOptions {
  id             String     @id @default(uuid())
  tenantId       String
  questionId     String
  optionOrder    Int        @default(1)
  optionLabel    String
  optionText     String
  attachmentId   String
  isCorrect      Boolean    @default(false)
  createdAt      DateTime   @default(now())
  createdBy      String
  updatedAt      DateTime   @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int        @default(1)
  tenant         Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers Users?     @relation("question_options_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers Users?     @relation("question_options_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers Users?     @relation("question_options_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([questionId, optionLabel])
  @@unique([questionId, version])
  @@unique([questionId])
}

model QuestionExplanations {
  id               String     @id @default(uuid())
  tenantId         String
  questionId       String
  language         String     @default("EN")
  solutionText     String
  shortExplanation String
  videoUrl         String
  aiGenerated      Boolean    @default(false)
  verified         Boolean    @default(false)
  verifiedBy       String
  verifiedAt       DateTime
  createdAt        DateTime   @default(now())
  createdBy        String
  updatedAt        DateTime   @default(now())
  updatedBy        String
  deletedAt        DateTime
  deletedBy        String
  version          Int        @default(1)
  tenant           Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  verifiedByusers  Users?     @relation("question_explanations_verified_by_to_users", fields: [verifiedBy], references: [id], onDelete: SetNull)
  createdByusers   Users?     @relation("question_explanations_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers   Users?     @relation("question_explanations_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers   Users?     @relation("question_explanations_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([questionId, language])
  @@unique([questionId, version])
  @@unique([questionId])
}

model QuestionTags {
  id             String      @id @default(uuid())
  tenantId       String
  tagName        String
  tagType        TagTypeEnum
  color          String
  description    String
  status         String      @default("ACTIVE")
  createdAt      DateTime    @default(now())
  createdBy      String
  updatedAt      DateTime    @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int         @default(1)
  tenant         Institutes  @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers Users?      @relation("question_tags_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers Users?      @relation("question_tags_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers Users?      @relation("question_tags_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([tenantId, tagName])
}

model QuestionTagMappings {
  id             String     @id @default(uuid())
  tenantId       String
  questionId     String
  tagId          String
  createdAt      DateTime   @default(now())
  createdBy      String
  updatedAt      DateTime   @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int        @default(1)
  tenant         Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers Users?     @relation("question_tag_mappings_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers Users?     @relation("question_tag_mappings_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers Users?     @relation("question_tag_mappings_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([questionId, tagId])
  @@unique([questionId, version])
  @@unique([questionId])
}

model QuestionVersions {
  id             String     @id @default(uuid())
  tenantId       String
  questionId     String
  version        Int
  snapshot       Json
  changeReason   String
  createdAt      DateTime   @default(now())
  createdBy      String
  tenant         Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers Users?     @relation("question_versions_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([questionId, version])
  @@unique([questionId])
}

model QuestionAttachments {
  id              String             @id @default(uuid())
  tenantId        String
  questionId      String
  storageObjectId String
  attachmentType  AttachmentTypeEnum
  displayOrder    Int                @default(1)
  caption         String
  createdAt       DateTime           @default(now())
  createdBy       String
  updatedAt       DateTime           @default(now())
  updatedBy       String
  deletedAt       DateTime
  deletedBy       String
  version         Int                @default(1)
  tenant          Institutes         @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers  Users?             @relation("question_attachments_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers  Users?             @relation("question_attachments_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers  Users?             @relation("question_attachments_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([questionId, version])
  @@unique([questionId])
}

model QuestionReviews {
  id              String           @id @default(uuid())
  tenantId        String
  questionId      String
  reviewerId      String
  reviewStatus    ReviewStatusEnum
  comments        String
  reviewedAt      DateTime
  createdAt       DateTime         @default(now())
  createdBy       String
  updatedAt       DateTime         @default(now())
  updatedBy       String
  deletedAt       DateTime
  deletedBy       String
  version         Int              @default(1)
  tenant          Institutes       @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  reviewerIdusers Users?           @relation("question_reviews_reviewer_id_to_users", fields: [reviewerId], references: [id], onDelete: SetNull)
  createdByusers  Users?           @relation("question_reviews_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers  Users?           @relation("question_reviews_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers  Users?           @relation("question_reviews_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([questionId, version])
  @@unique([questionId])
}

model QuestionUsage {
  id                String     @id @default(uuid())
  tenantId          String
  questionId        String
  examCount         Int        @default(0)
  attemptCount      Int        @default(0)
  correctCount      Int        @default(0)
  wrongCount        Int        @default(0)
  skippedCount      Int        @default(0)
  correctPercentage Decimal
  wrongPercentage   Decimal
  skippedPercentage Decimal
  avgTimeSeconds    Decimal
  maxTimeSeconds    Decimal
  minTimeSeconds    Decimal
  lastUsedAt        DateTime
  difficultyTrend   Json
  createdAt         DateTime   @default(now())
  updatedAt         DateTime   @default(now())
  deletedAt         DateTime
  deletedBy         String
  version           Int        @default(1)
  tenant            Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  deletedByusers    Users?     @relation("question_usage_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([questionId, version])
  @@unique([questionId])
}

model QuestionImportJobs {
  id                String                 @id @default(uuid())
  tenantId          String
  fileName          String
  fileType          String
  fileSizeBytes     BigInt
  storageObjectId   String
  subjectId         String
  chapterId         String
  defaultDifficulty QuestionDifficultyEnum
  defaultLanguage   String                 @default("EN")
  totalRows         Int                    @default(0)
  importedRows      Int                    @default(0)
  failedRows        Int                    @default(0)
  skippedRows       Int                    @default(0)
  status            ImportStatusEnum
  errorLog          Json
  startedAt         DateTime
  completedAt       DateTime
  createdAt         DateTime               @default(now())
  createdBy         String
  updatedAt         DateTime               @default(now())
  updatedBy         String
  deletedAt         DateTime
  deletedBy         String
  version           Int                    @default(1)
  tenant            Institutes             @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers    Users?                 @relation("question_import_jobs_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers    Users?                 @relation("question_import_jobs_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)
  deletedByusers    Users?                 @relation("question_import_jobs_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model LearningMaterials {
  id                String                     @id @default(uuid())
  tenantId          String
  materialCode      String
  courseId          String
  subjectId         String
  chapterId         String
  topicId           String
  batchId           String
  title             String
  description       String
  materialType      LearningMaterialTypeEnum
  language          String                     @default("EN")
  difficulty        QuestionDifficultyEnum
  estimatedDuration Int
  readingTime       Int
  status            LearningMaterialStatusEnum
  publishedAt       DateTime
  publishedBy       String
  visibility        MaterialVisibilityEnum
  embeddingId       String
  summary           String
  keywords          String[]
  aiMetadata        Json
  searchVector      String
  deletedAt         DateTime
  deletedBy         String
  createdAt         DateTime                   @default(now())
  createdBy         String
  updatedAt         DateTime                   @default(now())
  updatedBy         String
  version           Int                        @default(1)
  tenant            Institutes                 @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  publishedByusers  Users?                     @relation("learning_materials_published_by_to_users", fields: [publishedBy], references: [id], onDelete: SetNull)
  deletedByusers    Users?                     @relation("learning_materials_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
  createdByusers    Users?                     @relation("learning_materials_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers    Users?                     @relation("learning_materials_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([tenantId, materialCode])
}

model MaterialVersions {
  id               String     @id @default(uuid())
  tenantId         String
  materialId       String
  version          Int
  materialSnapshot Json
  changeReason     String
  createdAt        DateTime   @default(now())
  createdBy        String
  tenant           Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByusers   Users?     @relation("material_versions_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([materialId, version])
  @@unique([materialId])
}

model MaterialAttachments {
  id              String                     @id @default(uuid())
  tenantId        String
  materialId      String
  storageObjectId String
  attachmentType  LearningAttachmentTypeEnum
  displayOrder    Int                        @default(1)
  duration        Int
  fileSize        BigInt
  mimeType        String
  caption         String
  deletedAt       DateTime
  deletedBy       String
  createdAt       DateTime                   @default(now())
  createdBy       String
  updatedAt       DateTime                   @default(now())
  updatedBy       String
  version         Int                        @default(1)
  tenant          Institutes                 @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  deletedByusers  Users?                     @relation("material_attachments_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
  createdByusers  Users?                     @relation("material_attachments_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers  Users?                     @relation("material_attachments_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([materialId, version])
  @@unique([materialId])
}

model LearningPaths {
  id             String                 @id @default(uuid())
  tenantId       String
  title          String
  description    String
  difficulty     QuestionDifficultyEnum
  estimatedDays  Int
  status         LearningPathStatusEnum
  deletedAt      DateTime
  deletedBy      String
  createdAt      DateTime               @default(now())
  createdBy      String
  updatedAt      DateTime               @default(now())
  updatedBy      String
  version        Int                    @default(1)
  tenant         Institutes             @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  deletedByusers Users?                 @relation("learning_paths_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
  createdByusers Users?                 @relation("learning_paths_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers Users?                 @relation("learning_paths_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model LearningPathMaterials {
  id              String     @id @default(uuid())
  tenantId        String
  pathId          String
  materialId      String
  displayOrder    Int        @default(1)
  unlockAfterDays Int        @default(0)
  isMandatory     Boolean    @default(true)
  deletedAt       DateTime
  deletedBy       String
  createdAt       DateTime   @default(now())
  createdBy       String
  updatedAt       DateTime   @default(now())
  updatedBy       String
  version         Int        @default(1)
  tenant          Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  deletedByusers  Users?     @relation("learning_path_materials_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
  createdByusers  Users?     @relation("learning_path_materials_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers  Users?     @relation("learning_path_materials_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([materialId, version])
  @@unique([pathId, materialId])
  @@unique([pathId, displayOrder])
  @@unique([materialId])
}

model StudentLearningProgress {
  id                 String             @id @default(uuid())
  tenantId           String
  studentAdmissionId String
  materialId         String
  startedAt          DateTime
  completedAt        DateTime
  progressPercentage Decimal
  lastPosition       Int
  timeSpentSeconds   Int                @default(0)
  status             ProgressStatusEnum
  deletedAt          DateTime
  deletedBy          String
  createdAt          DateTime           @default(now())
  createdBy          String
  updatedAt          DateTime           @default(now())
  updatedBy          String
  version            Int                @default(1)
  tenant             Institutes         @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  deletedByusers     Users?             @relation("student_learning_progress_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
  createdByusers     Users?             @relation("student_learning_progress_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers     Users?             @relation("student_learning_progress_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([materialId, version])
  @@unique([studentAdmissionId, materialId])
  @@unique([materialId])
}

model LearningBookmarks {
  id             String     @id @default(uuid())
  tenantId       String
  studentId      String
  materialId     String
  pageNumber     Int
  videoTimestamp Int
  notes          String
  deletedAt      DateTime
  deletedBy      String
  createdAt      DateTime   @default(now())
  createdBy      String
  updatedAt      DateTime   @default(now())
  updatedBy      String
  version        Int        @default(1)
  tenant         Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  studentIdusers Users?     @relation("learning_bookmarks_student_id_to_users", fields: [studentId], references: [id], onDelete: SetNull)
  deletedByusers Users?     @relation("learning_bookmarks_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
  createdByusers Users?     @relation("learning_bookmarks_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers Users?     @relation("learning_bookmarks_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([materialId, version])
  @@unique([materialId])
}

model LearningNotes {
  id             String     @id @default(uuid())
  tenantId       String
  studentId      String
  materialId     String
  content        String
  highlight      String
  pageNumber     Int
  videoTimestamp Int
  aiSummary      String
  deletedAt      DateTime
  deletedBy      String
  createdAt      DateTime   @default(now())
  createdBy      String
  updatedAt      DateTime   @default(now())
  updatedBy      String
  version        Int        @default(1)
  tenant         Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  studentIdusers Users?     @relation("learning_notes_student_id_to_users", fields: [studentId], references: [id], onDelete: SetNull)
  deletedByusers Users?     @relation("learning_notes_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
  createdByusers Users?     @relation("learning_notes_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers Users?     @relation("learning_notes_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([materialId, version])
  @@unique([materialId])
}

model MaterialDiscussions {
  id                             String                @id @default(uuid())
  tenantId                       String
  materialId                     String
  parentCommentId                String
  authorId                       String
  comment                        String
  status                         DiscussionStatusEnum
  deletedAt                      DateTime
  deletedBy                      String
  createdAt                      DateTime              @default(now())
  createdBy                      String
  updatedAt                      DateTime              @default(now())
  updatedBy                      String
  version                        Int                   @default(1)
  material_discussionss          MaterialDiscussions[] @relation("material_discussions_parent_comment_id_to_material_discussions")
  tenant                         Institutes            @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  parentCommentIdmaterial_discus MaterialDiscussions?  @relation("material_discussions_parent_comment_id_to_material_discussions", fields: [parentCommentId], references: [id], onDelete: SetNull)
  authorIdusers                  Users?                @relation("material_discussions_author_id_to_users", fields: [authorId], references: [id], onDelete: SetNull)
  deletedByusers                 Users?                @relation("material_discussions_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
  createdByusers                 Users?                @relation("material_discussions_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers                 Users?                @relation("material_discussions_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([materialId, version])
  @@unique([materialId])
}

model MaterialAssignments {
  id             String     @id @default(uuid())
  tenantId       String
  materialId     String
  questionBankId String
  dueDate        DateTime
  marks          Decimal
  submissionType String     @default("TEXT")
  instructions   String
  deletedAt      DateTime
  deletedBy      String
  createdAt      DateTime   @default(now())
  createdBy      String
  updatedAt      DateTime   @default(now())
  updatedBy      String
  version        Int        @default(1)
  tenant         Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  deletedByusers Users?     @relation("material_assignments_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
  createdByusers Users?     @relation("material_assignments_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers Users?     @relation("material_assignments_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([materialId, version])
  @@unique([materialId])
}

model AssignmentSubmissions {
  id                 String     @id @default(uuid())
  tenantId           String
  assignmentId       String
  studentAdmissionId String
  attemptNumber      Int        @default(1)
  isLatest           Boolean    @default(true)
  submittedAt        DateTime
  status             String     @default("PENDING")
  marks              Decimal
  feedback           String
  evaluatedBy        String
  evaluatedAt        DateTime
  submittedByAi      Boolean    @default(false)
  lateSubmission     Boolean    @default(false)
  storageObjectId    String
  deletedAt          DateTime
  deletedBy          String
  createdAt          DateTime   @default(now())
  createdBy          String
  updatedAt          DateTime   @default(now())
  updatedBy          String
  version            Int        @default(1)
  tenant             Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  evaluatedByusers   Users?     @relation("assignment_submissions_evaluated_by_to_users", fields: [evaluatedBy], references: [id], onDelete: SetNull)
  deletedByusers     Users?     @relation("assignment_submissions_deleted_by_to_users", fields: [deletedBy], references: [id], onDelete: SetNull)
  createdByusers     Users?     @relation("assignment_submissions_created_by_to_users", fields: [createdBy], references: [id], onDelete: SetNull)
  updatedByusers     Users?     @relation("assignment_submissions_updated_by_to_users", fields: [updatedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([assignmentId, studentAdmissionId, attemptNumber])
}

model LiveClasses {
  id                  String              @id @default(uuid())
  tenantId            String
  courseId            String
  academicYearId      String
  batchId             String
  subjectId           String
  chapterId           String
  topicId             String
  questionPaperId     String
  title               String
  subtitle            String
  description         String
  learningObjectives  String[]
  prerequisites       String[]
  teacherNotes        String
  classType           LiveClassTypeEnum
  sessionType         SessionTypeEnum
  meetingProvider     MeetingProviderEnum
  meetingCode         String
  meetingPassword     String
  waitingRoomEnabled  Boolean             @default(true)
  lobbyEnabled        Boolean             @default(false)
  recordingEnabled    Boolean             @default(true)
  chatEnabled         Boolean             @default(true)
  screenShareEnabled  Boolean             @default(true)
  whiteboardEnabled   Boolean             @default(true)
  breakoutRoomEnabled Boolean             @default(false)
  scheduledStart      DateTime
  scheduledEnd        DateTime
  actualStart         DateTime
  actualEnd           DateTime
  durationMinutes     Int
  maximumStudents     Int                 @default(500)
  minimumStudents     Int                 @default(1)
  allowWaitlist       Boolean             @default(false)
  waitlistCount       Int                 @default(0)
  status              LiveClassStatusEnum
  publishedAt         DateTime
  publishedBy         String
  cancelledAt         DateTime
  cancelledBy         String
  cancelReason        String
  createdAt           DateTime            @default(now())
  createdBy           String
  updatedAt           DateTime            @default(now())
  updatedBy           String
  deletedAt           DateTime
  deletedBy           String
  version             Int                 @default(1)
  tenant              Institutes          @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model LiveClassInstructors {
  id                           String              @id @default(uuid())
  tenantId                     String
  liveClassId                  String
  staffProfileId               String
  role                         ParticipantRoleEnum
  isPrimary                    Boolean             @default(false)
  displayOrder                 Int                 @default(1)
  joinedAt                     DateTime
  leftAt                       DateTime
  createdAt                    DateTime            @default(now())
  createdBy                    String
  updatedAt                    DateTime            @default(now())
  updatedBy                    String
  deletedAt                    DateTime
  deletedBy                    String
  version                      Int                 @default(1)
  tenant                       Institutes          @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  staffProfileIdstaff_profiles StaffProfiles?      @relation("live_class_instructors_staff_profile_id_to_staff_profiles", fields: [staffProfileId], references: [userId], onDelete: SetNull)

  @@unique([tenantId, id])
}

model LiveClassSessions {
  id                  String               @id @default(uuid())
  tenantId            String
  liveClassId         String
  sessionNumber       Int                  @default(1)
  providerSessionId   String
  providerMetadata    Json
  status              SessionStatusEnum
  endedReason         SessionEndReasonEnum
  startedAt           DateTime
  endedAt             DateTime
  hostJoinedAt        DateTime
  hostLeftAt          DateTime
  peakParticipants    Int                  @default(0)
  networkQualityScore Decimal
  createdAt           DateTime             @default(now())
  createdBy           String
  updatedAt           DateTime             @default(now())
  updatedBy           String
  deletedAt           DateTime
  deletedBy           String
  version             Int                  @default(1)
  tenant              Institutes           @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model LiveClassParticipants {
  id                 String     @id @default(uuid())
  tenantId           String
  sessionId          String
  studentAdmissionId String
  deviceType         String
  browser            String
  os                 String
  ipAddress          String
  networkType        String
  joinedAt           DateTime   @default(now())
  leftAt             DateTime
  cameraEnabled      Boolean    @default(false)
  micEnabled         Boolean    @default(false)
  createdAt          DateTime   @default(now())
  createdBy          String
  updatedAt          DateTime   @default(now())
  updatedBy          String
  deletedAt          DateTime
  deletedBy          String
  version            Int        @default(1)
  tenant             Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model LiveClassAttendance {
  id                     String               @id @default(uuid())
  tenantId               String
  sessionId              String
  studentAdmissionId     String
  attendanceStatus       AttendanceStatusEnum
  totalDurationSeconds   Int                  @default(0)
  lateMinutes            Int                  @default(0)
  earlyLeaveMinutes      Int                  @default(0)
  networkDisconnectCount Int                  @default(0)
  cameraOnPercentage     Decimal
  micOnPercentage        Decimal
  attentionScore         Decimal
  focusScore             Decimal
  manualOverride         Boolean              @default(false)
  markedBy               String
  markedAt               DateTime
  remarks                String
  createdAt              DateTime             @default(now())
  createdBy              String
  updatedAt              DateTime             @default(now())
  updatedBy              String
  deletedAt              DateTime
  deletedBy              String
  version                Int                  @default(1)
  tenant                 Institutes           @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  markedByusers          Users?               @relation("live_class_attendance_marked_by_to_users", fields: [markedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model LiveClassRecordings {
  id                    String              @id @default(uuid())
  tenantId              String
  sessionId             String
  storageObjectId       String
  thumbnailObjectId     String
  transcriptObjectId    String
  subtitleObjectId      String
  durationSeconds       Int                 @default(0)
  resolution            String
  bitrateKbps           Int
  fileSizeBytes         BigInt
  status                RecordingStatusEnum
  processingStartedAt   DateTime
  processingCompletedAt DateTime
  transcriptCompletedAt DateTime
  aiSummaryCompletedAt  DateTime
  createdAt             DateTime            @default(now())
  createdBy             String
  updatedAt             DateTime            @default(now())
  updatedBy             String
  deletedAt             DateTime
  deletedBy             String
  version               Int                 @default(1)
  tenant                Institutes          @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model LiveClassResources {
  id              String           @id @default(uuid())
  tenantId        String
  liveClassId     String
  sessionId       String
  storageObjectId String
  resourceType    ResourceTypeEnum
  title           String
  description     String
  displayOrder    Int              @default(1)
  isDownloadable  Boolean          @default(true)
  isPublic        Boolean          @default(true)
  availableFrom   DateTime
  availableUntil  DateTime
  uploadedBy      String
  createdAt       DateTime         @default(now())
  createdBy       String
  updatedAt       DateTime         @default(now())
  updatedBy       String
  deletedAt       DateTime
  deletedBy       String
  version         Int              @default(1)
  tenant          Institutes       @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  uploadedByusers Users?           @relation("live_class_resources_uploaded_by_to_users", fields: [uploadedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model LiveClassChatMessages {
  id                             String                  @id @default(uuid())
  tenantId                       String
  sessionId                      String
  replyToMessageId               String
  senderId                       String
  senderRole                     ParticipantRoleEnum
  messageType                    ChatMessageTypeEnum
  message                        String
  attachmentId                   String
  isPinned                       Boolean                 @default(false)
  isEdited                       Boolean                 @default(false)
  reactionCount                  Json
  editedAt                       DateTime
  createdAt                      DateTime                @default(now())
  createdBy                      String
  updatedAt                      DateTime                @default(now())
  updatedBy                      String
  deletedAt                      DateTime
  deletedBy                      String
  version                        Int                     @default(1)
  live_class_chat_messagess      LiveClassChatMessages[] @relation("live_class_chat_messages_reply_to_message_id_to_live_class_chat_messages")
  tenant                         Institutes              @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  replyToMessageIlive_class_chat LiveClassChatMessages?  @relation("live_class_chat_messages_reply_to_message_id_to_live_class_chat_messages", fields: [replyToMessageId], references: [id], onDelete: SetNull)
  senderIdusers                  Users?                  @relation("live_class_chat_messages_sender_id_to_users", fields: [senderId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model LiveClassPolls {
  id                      String         @id @default(uuid())
  tenantId                String
  sessionId               String
  question                String
  options                 Json
  pollType                PollTypeEnum
  correctAnswer           String
  allowMultiple           Boolean        @default(false)
  anonymous               Boolean        @default(false)
  timeLimitSeconds        Int
  showResultsLive         Boolean        @default(true)
  publishResultAfterClose Boolean        @default(true)
  status                  PollStatusEnum
  startsAt                DateTime
  endsAt                  DateTime
  createdAt               DateTime       @default(now())
  createdBy               String
  updatedAt               DateTime       @default(now())
  updatedBy               String
  deletedAt               DateTime
  deletedBy               String
  version                 Int            @default(1)
  tenant                  Institutes     @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model LiveClassPollResponses {
  id                 String     @id @default(uuid())
  tenantId           String
  pollId             String
  studentAdmissionId String
  selectedOption     String
  timeTakenSeconds   Int
  isCorrect          Boolean
  createdAt          DateTime   @default(now())
  createdBy          String
  updatedAt          DateTime   @default(now())
  updatedBy          String
  deletedAt          DateTime
  deletedBy          String
  version            Int        @default(1)
  tenant             Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model LiveClassWhiteboardSnapshots {
  id              String     @id @default(uuid())
  tenantId        String
  sessionId       String
  storageObjectId String
  pageNumber      Int        @default(1)
  drawingVersion  Int        @default(1)
  ocrStatus       String     @default("PENDING")
  ocrText         String
  embeddingId     String
  capturedAt      DateTime   @default(now())
  capturedBy      String
  createdAt       DateTime   @default(now())
  createdBy       String
  updatedAt       DateTime   @default(now())
  updatedBy       String
  deletedAt       DateTime
  deletedBy       String
  version         Int        @default(1)
  tenant          Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  capturedByusers Users?     @relation("live_class_whiteboard_snapshots_captured_by_to_users", fields: [capturedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model LiveClassRaiseHands {
  id                      String              @id @default(uuid())
  tenantId                String
  sessionId               String
  studentAdmissionId      String
  status                  RaiseHandStatusEnum
  raisedAt                DateTime            @default(now())
  respondedAt             DateTime
  respondedBy             String
  responseDurationSeconds Int
  remarks                 String
  createdAt               DateTime            @default(now())
  createdBy               String
  updatedAt               DateTime            @default(now())
  updatedBy               String
  deletedAt               DateTime
  deletedBy               String
  version                 Int                 @default(1)
  tenant                  Institutes          @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  respondedByusers        Users?              @relation("live_class_raise_hands_responded_by_to_users", fields: [respondedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model LiveClassBreakoutRooms {
  id              String     @id @default(uuid())
  tenantId        String
  sessionId       String
  roomName        String
  providerRoomId  String
  topicFocus      String
  maxParticipants Int        @default(20)
  startedAt       DateTime
  endedAt         DateTime
  createdAt       DateTime   @default(now())
  createdBy       String
  updatedAt       DateTime   @default(now())
  updatedBy       String
  deletedAt       DateTime
  deletedBy       String
  version         Int        @default(1)
  tenant          Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model BreakoutRoomParticipants {
  id                 String     @id @default(uuid())
  tenantId           String
  breakoutRoomId     String
  studentAdmissionId String
  joinedAt           DateTime   @default(now())
  leftAt             DateTime
  createdAt          DateTime   @default(now())
  createdBy          String
  updatedAt          DateTime   @default(now())
  updatedBy          String
  deletedAt          DateTime
  deletedBy          String
  version            Int        @default(1)
  tenant             Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model LiveClassEvents {
  id               String                 @id @default(uuid())
  tenantId         String
  sessionId        String
  eventType        LiveClassEventTypeEnum
  eventPayload     Json
  triggeredBy      String
  occurredAt       DateTime               @default(now())
  createdAt        DateTime               @default(now())
  createdBy        String
  updatedAt        DateTime               @default(now())
  updatedBy        String
  deletedAt        DateTime
  deletedBy        String
  version          Int                    @default(1)
  tenant           Institutes             @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  triggeredByusers Users?                 @relation("live_class_events_triggered_by_to_users", fields: [triggeredBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model FeeStructures {
  id             String                 @id @default(uuid())
  tenantId       String
  courseId       String
  academicYearId String
  branchId       String
  departmentId   String
  code           String
  name           String
  description    String
  effectiveFrom  DateTime
  effectiveTo    DateTime
  status         FeeStructureStatusEnum
  createdAt      DateTime               @default(now())
  createdBy      String
  updatedAt      DateTime               @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int                    @default(1)
  tenant         Institutes             @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, branchId, departmentId])
  @@unique([tenantId, code])
}

model FeeStructureItems {
  id             String     @id @default(uuid())
  tenantId       String
  feeStructureId String
  itemName       String
  amount         Decimal
  taxPercentage  Decimal
  displayOrder   Int        @default(1)
  mandatory      Boolean    @default(true)
  refundable     Boolean    @default(false)
  createdAt      DateTime   @default(now())
  createdBy      String
  updatedAt      DateTime   @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int        @default(1)
  tenant         Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model StudentFeeAssignments {
  id                 String     @id @default(uuid())
  tenantId           String
  studentAdmissionId String
  feeStructureId     String
  assignedDate       DateTime   @default(now())
  effectiveDate      DateTime   @default(now())
  baseAmount         Decimal
  taxAmount          Decimal
  discountAmount     Decimal
  adjustmentAmount   Decimal
  finalAmount        Decimal
  assignedBy         String
  remarks            String
  createdAt          DateTime   @default(now())
  createdBy          String
  updatedAt          DateTime   @default(now())
  updatedBy          String
  deletedAt          DateTime
  deletedBy          String
  version            Int        @default(1)
  tenant             Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  assignedByusers    Users?     @relation("student_fee_assignments_assigned_by_to_users", fields: [assignedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model FeeInstallments {
  id                String     @id @default(uuid())
  tenantId          String
  feeStructureId    String
  installmentNumber Int        @default(1)
  offsetDays        Int        @default(0)
  amountPercentage  Decimal
  graceDays         Int        @default(0)
  lateFeeRuleId     String
  createdAt         DateTime   @default(now())
  createdBy         String
  updatedAt         DateTime   @default(now())
  updatedBy         String
  deletedAt         DateTime
  deletedBy         String
  version           Int        @default(1)
  tenant            Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model StudentFeeInstallments {
  id                     String                @id @default(uuid())
  tenantId               String
  studentFeeAssignmentId String
  feeInstallmentId       String
  installmentNumber      Int                   @default(1)
  dueDate                DateTime
  baseAmount             Decimal
  taxAmount              Decimal
  discountAmount         Decimal
  penaltyAmount          Decimal
  finalAmount            Decimal
  paidAmount             Decimal
  balanceAmount          Decimal
  status                 InstallmentStatusEnum
  createdAt              DateTime              @default(now())
  createdBy              String
  updatedAt              DateTime              @default(now())
  updatedBy              String
  deletedAt              DateTime
  deletedBy              String
  version                Int                   @default(1)
  tenant                 Institutes            @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model FeePayments {
  id                      String            @id @default(uuid())
  tenantId                String
  studentFeeInstallmentId String
  collectionCenterId      String
  closureId               String
  financialPeriodId       String
  paymentDate             DateTime          @default(now())
  amount                  Decimal
  paymentMethod           PaymentMethodEnum
  referenceNumber         String
  receivedBy              String
  remarks                 String
  createdAt               DateTime          @default(now())
  createdBy               String
  updatedAt               DateTime          @default(now())
  updatedBy               String
  deletedAt               DateTime
  deletedBy               String
  version                 Int               @default(1)
  tenant                  Institutes        @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  receivedByusers         Users?            @relation("fee_payments_received_by_to_users", fields: [receivedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model PaymentTransactions {
  id                   String                @id @default(uuid())
  tenantId             String
  paymentId            String
  gatewayName          String
  gatewayTransactionId String
  amount               Decimal
  currency             String                @default("INR")
  status               TransactionStatusEnum
  failureReason        String
  retryCount           Int                   @default(0)
  gatewayResponse      Json
  createdAt            DateTime              @default(now())
  createdBy            String
  updatedAt            DateTime              @default(now())
  updatedBy            String
  deletedAt            DateTime
  deletedBy            String
  version              Int                   @default(1)
  tenant               Institutes            @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model PaymentRefunds {
  id              String           @id @default(uuid())
  tenantId        String
  paymentId       String
  refundAmount    Decimal
  reason          String
  status          RefundStatusEnum
  approvedBy      String
  refundedAt      DateTime
  gatewayRefundId String
  createdAt       DateTime         @default(now())
  createdBy       String
  updatedAt       DateTime         @default(now())
  updatedBy       String
  deletedAt       DateTime
  deletedBy       String
  version         Int              @default(1)
  tenant          Institutes       @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  approvedByusers Users?           @relation("payment_refunds_approved_by_to_users", fields: [approvedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model FeeDiscounts {
  id                 String     @id @default(uuid())
  tenantId           String
  code               String
  name               String
  description        String
  discountPercentage Decimal
  discountAmount     Decimal
  maxDiscountLimit   Decimal
  validFrom          DateTime
  validTo            DateTime
  createdAt          DateTime   @default(now())
  createdBy          String
  updatedAt          DateTime   @default(now())
  updatedBy          String
  deletedAt          DateTime
  deletedBy          String
  version            Int        @default(1)
  tenant             Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model StudentFeeDiscounts {
  id                     String     @id @default(uuid())
  tenantId               String
  studentFeeAssignmentId String
  discountId             String
  scholarshipId          String
  discountAmount         Decimal
  approvedBy             String
  approvedAt             DateTime
  remarks                String
  createdAt              DateTime   @default(now())
  createdBy              String
  updatedAt              DateTime   @default(now())
  updatedBy              String
  deletedAt              DateTime
  deletedBy              String
  version                Int        @default(1)
  tenant                 Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  approvedByusers        Users?     @relation("student_fee_discounts_approved_by_to_users", fields: [approvedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model FeePenalties {
  id              String     @id @default(uuid())
  tenantId        String
  code            String
  name            String
  description     String
  flatAmount      Decimal
  amountPerDay    Decimal
  maxPenaltyLimit Decimal
  graceDays       Int        @default(0)
  createdAt       DateTime   @default(now())
  createdBy       String
  updatedAt       DateTime   @default(now())
  updatedBy       String
  deletedAt       DateTime
  deletedBy       String
  version         Int        @default(1)
  tenant          Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model StudentFeePenalties {
  id                      String     @id @default(uuid())
  tenantId                String
  studentFeeInstallmentId String
  penaltyId               String
  lateDays                Int        @default(0)
  amount                  Decimal
  isWaived                Boolean    @default(false)
  waivedBy                String
  waivedAt                DateTime
  waiveReason             String
  createdAt               DateTime   @default(now())
  createdBy               String
  updatedAt               DateTime   @default(now())
  updatedBy               String
  deletedAt               DateTime
  deletedBy               String
  version                 Int        @default(1)
  tenant                  Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  waivedByusers           Users?     @relation("student_fee_penalties_waived_by_to_users", fields: [waivedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model FeeReceipts {
  id              String     @id @default(uuid())
  tenantId        String
  paymentId       String
  receiptNumber   String
  storageObjectId String
  downloadCount   Int        @default(0)
  generatedAt     DateTime   @default(now())
  createdAt       DateTime   @default(now())
  createdBy       String
  updatedAt       DateTime   @default(now())
  updatedBy       String
  deletedAt       DateTime
  deletedBy       String
  version         Int        @default(1)
  tenant          Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model Scholarships {
  id                    String     @id @default(uuid())
  tenantId              String
  name                  String
  criteria              String
  scholarshipPercentage Decimal
  scholarshipAmount     Decimal
  maxAmountLimit        Decimal
  validFrom             DateTime
  validTo               DateTime
  createdAt             DateTime   @default(now())
  createdBy             String
  updatedAt             DateTime   @default(now())
  updatedBy             String
  deletedAt             DateTime
  deletedBy             String
  version               Int        @default(1)
  tenant                Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model FeeWaivers {
  id                      String     @id @default(uuid())
  tenantId                String
  studentFeeInstallmentId String
  waiverAmount            Decimal
  reason                  String
  approvedBy              String
  approvedAt              DateTime   @default(now())
  createdAt               DateTime   @default(now())
  createdBy               String
  updatedAt               DateTime   @default(now())
  updatedBy               String
  deletedAt               DateTime
  deletedBy               String
  version                 Int        @default(1)
  tenant                  Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  approvedByusers         Users?     @relation("fee_waivers_approved_by_to_users", fields: [approvedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model PaymentReconciliation {
  id                String                   @id @default(uuid())
  tenantId          String
  paymentId         String
  transactionId     String
  reconciledDate    DateTime                 @default(now())
  status            ReconciliationStatusEnum
  bankStatementRef  String
  expectedAmount    Decimal
  actualAmount      Decimal
  discrepancyAmount Decimal
  reconciledBy      String
  remarks           String
  createdAt         DateTime                 @default(now())
  createdBy         String
  updatedAt         DateTime                 @default(now())
  updatedBy         String
  deletedAt         DateTime
  deletedBy         String
  version           Int                      @default(1)
  tenant            Institutes               @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  reconciledByusers Users?                   @relation("payment_reconciliation_reconciled_by_to_users", fields: [reconciledBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model FeeNotifications {
  id                      String                     @id @default(uuid())
  tenantId                String
  studentFeeInstallmentId String
  notificationChannel     FeeNotificationChannelEnum
  recipientAddress        String
  sentAt                  DateTime                   @default(now())
  status                  FeeNotificationStatusEnum
  failureReason           String
  retryCount              Int                        @default(0)
  createdAt               DateTime                   @default(now())
  createdBy               String
  updatedAt               DateTime                   @default(now())
  updatedBy               String
  deletedAt               DateTime
  deletedBy               String
  version                 Int                        @default(1)
  tenant                  Institutes                 @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model FeeAuditLogs {
  id                 String                @id @default(uuid())
  tenantId           String
  studentAdmissionId String
  eventType          FeeAuditEventTypeEnum
  description        String
  payload            Json
  triggeredBy        String
  occurredAt         DateTime              @default(now())
  createdAt          DateTime              @default(now())
  createdBy          String
  updatedAt          DateTime              @default(now())
  updatedBy          String
  deletedAt          DateTime
  deletedBy          String
  version            Int                   @default(1)
  tenant             Institutes            @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  triggeredByusers   Users?                @relation("fee_audit_logs_triggered_by_to_users", fields: [triggeredBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model FeeAdjustments {
  id                     String             @id @default(uuid())
  tenantId               String
  studentFeeAssignmentId String
  adjustmentType         AdjustmentTypeEnum
  amount                 Decimal
  reason                 String
  adjustedBy             String
  adjustedAt             DateTime           @default(now())
  createdAt              DateTime           @default(now())
  createdBy              String
  updatedAt              DateTime           @default(now())
  updatedBy              String
  deletedAt              DateTime
  deletedBy              String
  version                Int                @default(1)
  tenant                 Institutes         @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  adjustedByusers        Users?             @relation("fee_adjustments_adjusted_by_to_users", fields: [adjustedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model PaymentAllocations {
  id                      String     @id @default(uuid())
  tenantId                String
  paymentId               String
  studentFeeInstallmentId String
  feeStructureItemId      String
  amount                  Decimal
  allocatedAt             DateTime   @default(now())
  createdAt               DateTime   @default(now())
  createdBy               String
  updatedAt               DateTime   @default(now())
  updatedBy               String
  deletedAt               DateTime
  deletedBy               String
  version                 Int        @default(1)
  tenant                  Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model FeeCollectionClosures {
  id                String            @id @default(uuid())
  tenantId          String
  cashierId         String
  openedAt          DateTime          @default(now())
  closedAt          DateTime
  status            ClosureStatusEnum
  openingBalance    Decimal
  expectedAmount    Decimal
  actualAmount      Decimal
  discrepancyAmount Decimal
  remarks           String
  createdAt         DateTime          @default(now())
  createdBy         String
  updatedAt         DateTime          @default(now())
  updatedBy         String
  deletedAt         DateTime
  deletedBy         String
  version           Int               @default(1)
  tenant            Institutes        @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  cashierIdusers    Users?            @relation("fee_collection_closures_cashier_id_to_users", fields: [cashierId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model FeeCollectionCenters {
  id        String     @id @default(uuid())
  tenantId  String
  branchId  String
  code      String
  name      String
  isOnline  Boolean    @default(false)
  createdAt DateTime   @default(now())
  createdBy String
  updatedAt DateTime   @default(now())
  updatedBy String
  deletedAt DateTime
  deletedBy String
  version   Int        @default(1)
  tenant    Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model FinancialPeriodsMapping {
  id        String     @id @default(uuid())
  tenantId  String
  code      String
  name      String
  startDate DateTime
  endDate   DateTime
  isClosed  Boolean    @default(false)
  createdAt DateTime   @default(now())
  createdBy String
  updatedAt DateTime   @default(now())
  updatedBy String
  deletedAt DateTime
  deletedBy String
  version   Int        @default(1)
  tenant    Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model NotificationTemplates {
  id              String                      @id @default(uuid())
  tenantId        String
  code            String
  name            String
  description     String
  defaultChannel  NotificationChannelTypeEnum
  fallbackChannel NotificationChannelTypeEnum
  isActive        Boolean                     @default(true)
  createdAt       DateTime                    @default(now())
  createdBy       String
  updatedAt       DateTime                    @default(now())
  updatedBy       String
  deletedAt       DateTime
  deletedBy       String
  version         Int                         @default(1)
  tenant          Institutes                  @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model NotificationTemplateVersions {
  id                     String     @id @default(uuid())
  tenantId               String
  notificationTemplateId String
  versionNumber          Int        @default(1)
  subjectTemplate        String
  bodyTemplate           String
  isApproved             Boolean    @default(false)
  approvedBy             String
  createdAt              DateTime   @default(now())
  createdBy              String
  updatedAt              DateTime   @default(now())
  updatedBy              String
  deletedAt              DateTime
  deletedBy              String
  version                Int        @default(1)
  tenant                 Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  approvedByusers        Users?     @relation("notification_template_versions_approved_by_to_users", fields: [approvedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model NotificationCampaigns {
  id                     String             @id @default(uuid())
  tenantId               String
  notificationTemplateId String
  templateVersionId      String
  name                   String
  description            String
  status                 CampaignStatusEnum
  scheduledAt            DateTime
  startedAt              DateTime
  completedAt            DateTime
  createdAt              DateTime           @default(now())
  createdBy              String
  updatedAt              DateTime           @default(now())
  updatedBy              String
  deletedAt              DateTime
  deletedBy              String
  version                Int                @default(1)
  tenant                 Institutes         @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model NotificationCampaignTargets {
  id                     String     @id @default(uuid())
  tenantId               String
  notificationCampaignId String
  recipientUserId        String
  recipientVariables     Json
  isProcessed            Boolean    @default(false)
  processedAt            DateTime
  createdAt              DateTime   @default(now())
  createdBy              String
  updatedAt              DateTime   @default(now())
  updatedBy              String
  deletedAt              DateTime
  deletedBy              String
  version                Int        @default(1)
  tenant                 Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  recipientUserIdusers   Users?     @relation("notification_campaign_targets_recipient_user_id_to_users", fields: [recipientUserId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model NotificationQueue {
  id                     String                      @id @default(uuid())
  tenantId               String
  notificationTemplateId String
  templateVersionId      String
  notificationCampaignId String
  recipientUserId        String
  recipientAddress       String
  subject                String
  body                   String
  channelType            NotificationChannelTypeEnum
  status                 QueueStatusEnum
  retryCount             Int                         @default(0)
  maxRetries             Int                         @default(3)
  scheduledSendAt        DateTime                    @default(now())
  sentAt                 DateTime
  createdAt              DateTime                    @default(now())
  createdBy              String
  updatedAt              DateTime                    @default(now())
  updatedBy              String
  deletedAt              DateTime
  deletedBy              String
  version                Int                         @default(1)
  tenant                 Institutes                  @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  recipientUserIdusers   Users?                      @relation("notification_queue_recipient_user_id_to_users", fields: [recipientUserId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model NotificationDeliveries {
  id                  String             @id @default(uuid())
  tenantId            String
  notificationQueueId String
  providerName        String
  providerMessageId   String
  status              DeliveryStatusEnum
  sentAt              DateTime
  deliveredAt         DateTime
  readAt              DateTime
  deliveryLatencyMs   Int
  estimatedCost       Decimal
  providerResponse    Json
  errorMessage        String
  createdAt           DateTime           @default(now())
  createdBy           String
  updatedAt           DateTime           @default(now())
  updatedBy           String
  deletedAt           DateTime
  deletedBy           String
  version             Int                @default(1)
  tenant              Institutes         @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model NotificationChannels {
  code                String   @id
  name                String
  description         String
  supportsAttachments Boolean  @default(false)
  supportsTemplates   Boolean  @default(true)
  maxBodySize         Int
  isActive            Boolean  @default(true)
  displayOrder        Int      @default(0)
  createdAt           DateTime @default(now())
  updatedAt           DateTime @default(now())
  version             Int      @default(1)
  metadata            Json

  @@unique([code])
}

model NotificationPreferences {
  id          String                      @id @default(uuid())
  tenantId    String
  userId      String
  channelType NotificationChannelTypeEnum
  isEnabled   Boolean                     @default(true)
  createdAt   DateTime                    @default(now())
  createdBy   String
  updatedAt   DateTime                    @default(now())
  updatedBy   String
  deletedAt   DateTime
  deletedBy   String
  version     Int                         @default(1)
  tenant      Institutes                  @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  userIdusers Users?                      @relation("notification_preferences_user_id_to_users", fields: [userId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model NotificationSubscriptions {
  id           String     @id @default(uuid())
  tenantId     String
  userId       String
  topicName    String
  isSubscribed Boolean    @default(true)
  createdAt    DateTime   @default(now())
  createdBy    String
  updatedAt    DateTime   @default(now())
  updatedBy    String
  deletedAt    DateTime
  deletedBy    String
  version      Int        @default(1)
  tenant       Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  userIdusers  Users?     @relation("notification_subscriptions_user_id_to_users", fields: [userId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model NotificationLogs {
  id                   String                      @id @default(uuid())
  tenantId             String
  notificationQueueId  String
  recipientUserId      String
  channelType          NotificationChannelTypeEnum
  recipientAddress     String
  subject              String
  body                 String
  sentAt               DateTime                    @default(now())
  createdAt            DateTime                    @default(now())
  createdBy            String
  updatedAt            DateTime                    @default(now())
  updatedBy            String
  deletedAt            DateTime
  deletedBy            String
  version              Int                         @default(1)
  tenant               Institutes                  @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  recipientUserIdusers Users?                      @relation("notification_logs_recipient_user_id_to_users", fields: [recipientUserId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model SmsProviders {
  id             String     @id @default(uuid())
  tenantId       String
  code           String
  name           String
  apiEndpoint    String
  providerConfig Json
  isActive       Boolean    @default(true)
  priority       Int        @default(1)
  balanceCredits Decimal
  createdAt      DateTime   @default(now())
  createdBy      String
  updatedAt      DateTime   @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int        @default(1)
  tenant         Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model EmailProviders {
  id             String     @id @default(uuid())
  tenantId       String
  code           String
  name           String
  apiEndpoint    String
  providerConfig Json
  isActive       Boolean    @default(true)
  priority       Int        @default(1)
  createdAt      DateTime   @default(now())
  createdBy      String
  updatedAt      DateTime   @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int        @default(1)
  tenant         Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model WhatsappProviders {
  id             String     @id @default(uuid())
  tenantId       String
  code           String
  name           String
  apiEndpoint    String
  providerConfig Json
  isActive       Boolean    @default(true)
  priority       Int        @default(1)
  createdAt      DateTime   @default(now())
  createdBy      String
  updatedAt      DateTime   @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int        @default(1)
  tenant         Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model PushDevices {
  id           String             @id @default(uuid())
  tenantId     String
  userId       String
  deviceToken  String
  platform     DevicePlatformEnum
  isActive     Boolean            @default(true)
  lastActiveAt DateTime           @default(now())
  createdAt    DateTime           @default(now())
  createdBy    String
  updatedAt    DateTime           @default(now())
  updatedBy    String
  deletedAt    DateTime
  deletedBy    String
  version      Int                @default(1)
  tenant       Institutes         @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  userIdusers  Users?             @relation("push_devices_user_id_to_users", fields: [userId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model PushNotifications {
  id           String     @id @default(uuid())
  tenantId     String
  pushDeviceId String
  title        String
  body         String
  clickAction  String
  sentAt       DateTime   @default(now())
  openedAt     DateTime
  clickedAt    DateTime
  isDelivered  Boolean    @default(false)
  createdAt    DateTime   @default(now())
  createdBy    String
  updatedAt    DateTime   @default(now())
  updatedBy    String
  deletedAt    DateTime
  deletedBy    String
  version      Int        @default(1)
  tenant       Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model Announcements {
  id                 String               @id @default(uuid())
  tenantId           String
  courseId           String
  departmentId       String
  announcementType   AnnouncementTypeEnum
  title              String
  content            String
  publishedAt        DateTime             @default(now())
  expiresAt          DateTime
  createdByUser      String
  createdAt          DateTime             @default(now())
  createdBy          String
  updatedAt          DateTime             @default(now())
  updatedBy          String
  deletedAt          DateTime
  deletedBy          String
  version            Int                  @default(1)
  tenant             Institutes           @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  createdByUserusers Users?               @relation("announcements_created_by_user_to_users", fields: [createdByUser], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model AnnouncementReads {
  id             String     @id @default(uuid())
  tenantId       String
  announcementId String
  userId         String
  readAt         DateTime   @default(now())
  createdAt      DateTime   @default(now())
  createdBy      String
  updatedAt      DateTime   @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int        @default(1)
  tenant         Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  userIdusers    Users?     @relation("announcement_reads_user_id_to_users", fields: [userId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model WebhookEvents {
  id              String     @id @default(uuid())
  tenantId        String
  providerName    String
  eventType       String
  payload         Json
  processedStatus String     @default("PENDING")
  receivedAt      DateTime   @default(now())
  processedAt     DateTime
  createdAt       DateTime   @default(now())
  createdBy       String
  updatedAt       DateTime   @default(now())
  updatedBy       String
  deletedAt       DateTime
  deletedBy       String
  version         Int        @default(1)
  tenant          Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model WebhookDeliveries {
  id             String     @id @default(uuid())
  tenantId       String
  webhookEventId String
  attempts       Int        @default(1)
  status         String     @default("PROCESSING")
  errorLog       String
  processedAt    DateTime   @default(now())
  createdAt      DateTime   @default(now())
  createdBy      String
  updatedAt      DateTime   @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int        @default(1)
  tenant         Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model CommunicationAuditLogs {
  id               String                          @id @default(uuid())
  tenantId         String
  eventType        CommunicationAuditEventTypeEnum
  description      String
  payload          Json
  triggeredBy      String
  occurredAt       DateTime                        @default(now())
  createdAt        DateTime                        @default(now())
  createdBy        String
  updatedAt        DateTime                        @default(now())
  updatedBy        String
  deletedAt        DateTime
  deletedBy        String
  version          Int                             @default(1)
  tenant           Institutes                      @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  triggeredByusers Users?                          @relation("communication_audit_logs_triggered_by_to_users", fields: [triggeredBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model NotificationAttachments {
  id                  String     @id @default(uuid())
  tenantId            String
  notificationQueueId String
  storageObjectId     String
  fileName            String
  fileSizeBytes       Int
  mimeType            String
  createdAt           DateTime   @default(now())
  createdBy           String
  updatedAt           DateTime   @default(now())
  updatedBy           String
  deletedAt           DateTime
  deletedBy           String
  version             Int        @default(1)
  tenant              Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model NotificationVariables {
  id           String     @id @default(uuid())
  tenantId     String
  code         String
  name         String
  description  String
  defaultValue String
  createdAt    DateTime   @default(now())
  createdBy    String
  updatedAt    DateTime   @default(now())
  updatedBy    String
  deletedAt    DateTime
  deletedBy    String
  version      Int        @default(1)
  tenant       Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model NotificationRetryQueue {
  id                  String     @id @default(uuid())
  tenantId            String
  notificationQueueId String
  attemptNumber       Int        @default(1)
  nextAttemptAt       DateTime
  lastError           String
  backoffMultiplier   Decimal
  createdAt           DateTime   @default(now())
  createdBy           String
  updatedAt           DateTime   @default(now())
  updatedBy           String
  deletedAt           DateTime
  deletedBy           String
  version             Int        @default(1)
  tenant              Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model CommunicationRateLimits {
  id                 String     @id @default(uuid())
  tenantId           String
  limitScope         String
  scopeTargetCode    String
  maxMessagesAllowed Int
  windowSeconds      Int
  createdAt          DateTime   @default(now())
  createdBy          String
  updatedAt          DateTime   @default(now())
  updatedBy          String
  deletedAt          DateTime
  deletedBy          String
  version            Int        @default(1)
  tenant             Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model ProviderFailovers {
  id                           String                      @id @default(uuid())
  tenantId                     String
  channelType                  NotificationChannelTypeEnum
  primaryProviderId            String
  fallbackProviderId           String
  consecutiveFailuresThreshold Int                         @default(5)
  isTriggered                  Boolean                     @default(false)
  triggeredAt                  DateTime
  createdAt                    DateTime                    @default(now())
  createdBy                    String
  updatedAt                    DateTime                    @default(now())
  updatedBy                    String
  deletedAt                    DateTime
  deletedBy                    String
  version                      Int                         @default(1)
  tenant                       Institutes                  @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model DashboardDefinitions {
  id          String                  @id @default(uuid())
  tenantId    String
  code        String
  name        String
  description String
  targetRole  DashboardViewerRoleEnum
  isActive    Boolean                 @default(true)
  gridLayout  Json
  createdAt   DateTime                @default(now())
  createdBy   String
  updatedAt   DateTime                @default(now())
  updatedBy   String
  deletedAt   DateTime
  deletedBy   String
  version     Int                     @default(1)
  tenant      Institutes              @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model DashboardWidgets {
  id                    String         @id @default(uuid())
  tenantId              String
  dashboardDefinitionId String
  name                  String
  widgetType            WidgetTypeEnum
  rowIndex              Int            @default(0)
  colIndex              Int            @default(0)
  sizeWidth             Int            @default(4)
  sizeHeight            Int            @default(2)
  dataSourceQuery       String
  displayConfig         Json
  createdAt             DateTime       @default(now())
  createdBy             String
  updatedAt             DateTime       @default(now())
  updatedBy             String
  deletedAt             DateTime
  deletedBy             String
  version               Int            @default(1)
  tenant                Institutes     @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model ReportDefinitions {
  id               String     @id @default(uuid())
  tenantId         String
  code             String
  name             String
  description      String
  queryDefinition  String
  supportedFormats String     @default("'{PDF")
  isActive         Boolean    @default(true)
  createdAt        DateTime   @default(now())
  createdBy        String
  updatedAt        DateTime   @default(now())
  updatedBy        String
  deletedAt        DateTime
  deletedBy        String
  version          Int        @default(1)
  tenant           Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model ReportExecutions {
  id                 String           @id @default(uuid())
  tenantId           String
  reportDefinitionId String
  executionParams    Json
  formatType         ReportFormatEnum
  storageObjectId    String
  fileSizeBytes      BigInt
  generatedBy        String
  generatedAt        DateTime         @default(now())
  createdAt          DateTime         @default(now())
  createdBy          String
  updatedAt          DateTime         @default(now())
  updatedBy          String
  deletedAt          DateTime
  deletedBy          String
  version            Int              @default(1)
  tenant             Institutes       @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  generatedByusers   Users?           @relation("report_executions_generated_by_to_users", fields: [generatedBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model KpiDefinitions {
  id                String     @id @default(uuid())
  tenantId          String
  code              String
  name              String
  description       String
  formulaExpression String
  refreshFrequency  String     @default("DAILY")
  isActive          Boolean    @default(true)
  createdAt         DateTime   @default(now())
  createdBy         String
  updatedAt         DateTime   @default(now())
  updatedBy         String
  deletedAt         DateTime
  deletedBy         String
  version           Int        @default(1)
  tenant            Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model KpiSnapshots {
  id              String     @id @default(uuid())
  tenantId        String
  kpiDefinitionId String
  branchId        String
  courseId        String
  metricValue     Decimal
  snapshotDate    DateTime   @default(now())
  createdAt       DateTime   @default(now())
  createdBy       String
  updatedAt       DateTime   @default(now())
  updatedBy       String
  deletedAt       DateTime
  deletedBy       String
  version         Int        @default(1)
  tenant          Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model StudentAnalytics {
  id                      String     @id @default(uuid())
  tenantId                String
  studentAdmissionId      String
  totalAttendanceSessions Int        @default(0)
  presentSessionsCount    Int        @default(0)
  attendanceRatio         Decimal
  examsTakenCount         Int        @default(0)
  averageTestScore        Decimal
  highestScore            Decimal
  rankInBatch             Int
  totalAssignedFees       Decimal
  totalPaidFees           Decimal
  totalOutstandingBalance Decimal
  createdAt               DateTime   @default(now())
  createdBy               String
  updatedAt               DateTime   @default(now())
  updatedBy               String
  deletedAt               DateTime
  deletedBy               String
  version                 Int        @default(1)
  tenant                  Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model BranchAnalytics {
  id                     String     @id @default(uuid())
  tenantId               String
  branchId               String
  activeStudentsCount    Int        @default(0)
  averageTestScore       Decimal
  averageAttendanceRatio Decimal
  totalRevenue           Decimal
  totalOutstanding       Decimal
  createdAt              DateTime   @default(now())
  createdBy              String
  updatedAt              DateTime   @default(now())
  updatedBy              String
  deletedAt              DateTime
  deletedBy              String
  version                Int        @default(1)
  tenant                 Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model FinancialAnalytics {
  id                     String     @id @default(uuid())
  tenantId               String
  academicYearId         String
  branchId               String
  totalInvoicedAmount    Decimal
  totalReceivedAmount    Decimal
  totalOutstandingAmount Decimal
  totalRefundedAmount    Decimal
  totalWaivedAmount      Decimal
  createdAt              DateTime   @default(now())
  createdBy              String
  updatedAt              DateTime   @default(now())
  updatedBy              String
  deletedAt              DateTime
  deletedBy              String
  version                Int        @default(1)
  tenant                 Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model AnalyticsRefreshJobs {
  id                  String               @id @default(uuid())
  tenantId            String
  targetMetric        String
  status              RefreshJobStatusEnum
  startedAt           DateTime
  completedAt         DateTime
  executionDurationMs Int
  errorLog            String
  createdAt           DateTime             @default(now())
  createdBy           String
  updatedAt           DateTime             @default(now())
  updatedBy           String
  deletedAt           DateTime
  deletedBy           String
  version             Int                  @default(1)
  tenant              Institutes           @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model AnalyticsAuditLogs {
  id               String                  @id @default(uuid())
  tenantId         String
  eventType        AnalyticsAuditEventEnum
  description      String
  payload          Json
  triggeredBy      String
  occurredAt       DateTime                @default(now())
  createdAt        DateTime                @default(now())
  createdBy        String
  updatedAt        DateTime                @default(now())
  updatedBy        String
  deletedAt        DateTime
  deletedBy        String
  version          Int                     @default(1)
  tenant           Institutes              @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  triggeredByusers Users?                  @relation("analytics_audit_logs_triggered_by_to_users", fields: [triggeredBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model AiProviders {
  id                   String     @id @default(uuid())
  tenantId             String
  code                 String
  name                 String
  apiEndpoint          String
  providerConfig       Json
  isActive             Boolean    @default(true)
  priority             Int        @default(1)
  maxRequestsPerMinute Int
  createdAt            DateTime   @default(now())
  createdBy            String
  updatedAt            DateTime   @default(now())
  updatedBy            String
  deletedAt            DateTime
  deletedBy            String
  version              Int        @default(1)
  tenant               Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model AiModels {
  id                  String     @id @default(uuid())
  tenantId            String
  aiProviderId        String
  code                String
  name                String
  contextWindowTokens Int        @default(8192)
  inputTokenPriceUsd  Decimal
  outputTokenPriceUsd Decimal
  isActive            Boolean    @default(true)
  createdAt           DateTime   @default(now())
  createdBy           String
  updatedAt           DateTime   @default(now())
  updatedBy           String
  deletedAt           DateTime
  deletedBy           String
  version             Int        @default(1)
  tenant              Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model AiPrompts {
  id            String     @id @default(uuid())
  tenantId      String
  code          String
  name          String
  description   String
  systemPrompt  String
  userTemplate  String
  versionNumber Int        @default(1)
  isActive      Boolean    @default(true)
  createdAt     DateTime   @default(now())
  createdBy     String
  updatedAt     DateTime   @default(now())
  updatedBy     String
  deletedAt     DateTime
  deletedBy     String
  version       Int        @default(1)
  tenant        Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model AiConversations {
  id          String     @id @default(uuid())
  tenantId    String
  userId      String
  title       String     @default("New Conversation with AI Tutor")
  isArchived  Boolean    @default(false)
  createdAt   DateTime   @default(now())
  createdBy   String
  updatedAt   DateTime   @default(now())
  updatedBy   String
  deletedAt   DateTime
  deletedBy   String
  version     Int        @default(1)
  tenant      Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  userIdusers Users?     @relation("ai_conversations_user_id_to_users", fields: [userId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model AiMessages {
  id                   String              @id @default(uuid())
  tenantId             String
  aiConversationId     String
  role                 ChatMessageRoleEnum
  content              String
  tokensConsumed       Int
  responseTimeMs       Int
  aiGeneratedContentId String
  createdAt            DateTime            @default(now())
  createdBy            String
  updatedAt            DateTime            @default(now())
  updatedBy            String
  deletedAt            DateTime
  deletedBy            String
  version              Int                 @default(1)
  tenant               Institutes          @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model AiRequests {
  id                   String              @id @default(uuid())
  tenantId             String
  aiModelId            String
  promptTokens         Int                 @default(0)
  completionTokens     Int                 @default(0)
  totalTokens          Int
  executionDurationMs  Int
  estimatedCostUsd     Decimal
  status               AiRequestStatusEnum
  errorMessage         String
  triggeredByUser      String
  createdAt            DateTime            @default(now())
  createdBy            String
  updatedAt            DateTime            @default(now())
  updatedBy            String
  deletedAt            DateTime
  deletedBy            String
  version              Int                 @default(1)
  tenant               Institutes          @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  triggeredByUserusers Users?              @relation("ai_requests_triggered_by_user_to_users", fields: [triggeredByUser], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model AiGeneratedContent {
  id                  String     @id @default(uuid())
  tenantId            String
  contentType         String
  referenceEntityType String
  referenceEntityId   String
  generatedOutput     Json
  isPublished         Boolean    @default(true)
  createdAt           DateTime   @default(now())
  createdBy           String
  updatedAt           DateTime   @default(now())
  updatedBy           String
  deletedAt           DateTime
  deletedBy           String
  version             Int        @default(1)
  tenant              Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model AiFeedback {
  id          String     @id @default(uuid())
  tenantId    String
  aiMessageId String
  ratingScore Int
  comments    String
  userId      String
  createdAt   DateTime   @default(now())
  createdBy   String
  updatedAt   DateTime   @default(now())
  updatedBy   String
  deletedAt   DateTime
  deletedBy   String
  version     Int        @default(1)
  tenant      Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  userIdusers Users?     @relation("ai_feedback_user_id_to_users", fields: [userId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model AiUsageLogs {
  id             String     @id @default(uuid())
  tenantId       String
  userId         String
  usageDate      DateTime   @default(now())
  tokensConsumed Int        @default(0)
  requestsCount  Int        @default(0)
  createdAt      DateTime   @default(now())
  createdBy      String
  updatedAt      DateTime   @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int        @default(1)
  tenant         Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  userIdusers    Users?     @relation("ai_usage_logs_user_id_to_users", fields: [userId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model AiAuditLogs {
  id               String           @id @default(uuid())
  tenantId         String
  eventType        AiAuditEventEnum
  description      String
  payload          Json
  triggeredBy      String
  occurredAt       DateTime         @default(now())
  createdAt        DateTime         @default(now())
  createdBy        String
  updatedAt        DateTime         @default(now())
  updatedBy        String
  deletedAt        DateTime
  deletedBy        String
  version          Int              @default(1)
  tenant           Institutes       @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  triggeredByusers Users?           @relation("ai_audit_logs_triggered_by_to_users", fields: [triggeredBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model TenantSettings {
  id                    String     @id @default(uuid())
  tenantId              String
  timezone              String     @default("Asia/Kolkata")
  currency              String     @default("INR")
  locale                String     @default("en-IN")
  brandingConfig        Json
  passwordPolicy        Json
  sessionTimeoutSeconds Int        @default(3600)
  createdAt             DateTime   @default(now())
  createdBy             String
  updatedAt             DateTime   @default(now())
  updatedBy             String
  deletedAt             DateTime
  deletedBy             String
  version               Int        @default(1)
  tenant                Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model FeatureFlags {
  id                String   @id @default(uuid())
  tenantId          String
  featureKey        String
  enabled           Boolean  @default(false)
  rolloutPercentage Int      @default(100)
  planRequired      String
  beta              Boolean  @default(false)
  internal          Boolean  @default(false)
  deprecated        Boolean  @default(false)
  description       String
  createdAt         DateTime @default(now())
  createdBy         String
  updatedAt         DateTime @default(now())
  updatedBy         String
  deletedAt         DateTime
  deletedBy         String
  version           Int      @default(1)
  metadata          Json

  @@unique([tenantId, id])
  @@unique([tenantId, featureKey])
}

model StorageObjects {
  id               String     @id @default(uuid())
  tenantId         String
  bucketName       String
  filePath         String
  mimeType         String
  fileSizeBytes    BigInt
  md5Checksum      String
  virusScanPassed  Boolean    @default(true)
  ownerUserId      String
  isPublic         Boolean    @default(false)
  createdAt        DateTime   @default(now())
  createdBy        String
  updatedAt        DateTime   @default(now())
  updatedBy        String
  deletedAt        DateTime
  deletedBy        String
  version          Int        @default(1)
  tenant           Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  ownerUserIdusers Users?     @relation("storage_objects_owner_user_id_to_users", fields: [ownerUserId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model FileUploads {
  id              String     @id @default(uuid())
  tenantId        String
  fileName        String
  fileSizeBytes   BigInt
  mimeType        String
  uploadStatus    String     @default("PENDING")
  chunksTotal     Int        @default(1)
  chunksUploaded  Int        @default(0)
  storageObjectId String
  createdAt       DateTime   @default(now())
  createdBy       String
  updatedAt       DateTime   @default(now())
  updatedBy       String
  deletedAt       DateTime
  deletedBy       String
  version         Int        @default(1)
  tenant          Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model BackgroundJobs {
  id             String                  @id @default(uuid())
  tenantId       String
  jobType        String
  status         BackgroundJobStatusEnum
  payload        Json
  priority       Int                     @default(3)
  scheduledRunAt DateTime                @default(now())
  createdAt      DateTime                @default(now())
  createdBy      String
  updatedAt      DateTime                @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int                     @default(1)
  tenant         Institutes              @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model JobExecutions {
  id                  String     @id @default(uuid())
  tenantId            String
  backgroundJobId     String
  attemptNumber       Int        @default(1)
  status              String     @default("RUNNING")
  startedAt           DateTime   @default(now())
  completedAt         DateTime
  executionDurationMs Int
  memoryPeakBytes     BigInt
  errorMessage        String
  createdAt           DateTime   @default(now())
  createdBy           String
  updatedAt           DateTime   @default(now())
  updatedBy           String
  deletedAt           DateTime
  deletedBy           String
  version             Int        @default(1)
  tenant              Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model SchedulerJobs {
  id             String     @id @default(uuid())
  tenantId       String
  code           String
  name           String
  description    String
  cronExpression String
  jobPayload     Json
  isActive       Boolean    @default(true)
  createdAt      DateTime   @default(now())
  createdBy      String
  updatedAt      DateTime   @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int        @default(1)
  tenant         Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
}

model SchedulerHistory {
  id                  String     @id @default(uuid())
  tenantId            String
  schedulerJobId      String
  status              String     @default("RUNNING")
  startedAt           DateTime   @default(now())
  completedAt         DateTime
  executionDurationMs Int
  errorMessage        String
  createdAt           DateTime   @default(now())
  createdBy           String
  updatedAt           DateTime   @default(now())
  updatedBy           String
  deletedAt           DateTime
  deletedBy           String
  version             Int        @default(1)
  tenant              Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model ApiKeys {
  id             String   @id @default(uuid())
  tenantId       String
  service        String
  keyDisplayName String
  encryptedKey   String
  keyPrefix      String
  keyVersion     Int      @default(1)
  algorithm      String   @default("pgp-sym")
  expiresAt      DateTime
  lastUsedAt     DateTime
  lastRotatedAt  DateTime
  isActive       Boolean  @default(true)
  createdAt      DateTime @default(now())
  createdBy      String
  updatedAt      DateTime @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int      @default(1)
  metadata       Json

  @@unique([tenantId, id])
  @@unique([tenantId, service])
}

model ApiLogs {
  id              String     @id @default(uuid())
  tenantId        String
  integrationName String
  endpointUrl     String
  requestMethod   String     @default("POST")
  responseCode    Int
  latencyMs       Int
  requestPayload  Json
  responsePayload Json
  createdAt       DateTime   @default(now())
  createdBy       String
  updatedAt       DateTime   @default(now())
  updatedBy       String
  deletedAt       DateTime
  deletedBy       String
  version         Int        @default(1)
  tenant          Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model SystemHealthLogs {
  id                   String     @id @default(uuid())
  tenantId             String
  cpuUtilizationPct    Decimal
  memoryUtilizationPct Decimal
  diskUtilizationPct   Decimal
  dbConnectionsCount   Int
  redisConnected       Boolean    @default(true)
  metricsPayload       Json
  loggedAt             DateTime   @default(now())
  createdAt            DateTime   @default(now())
  createdBy            String
  updatedAt            DateTime   @default(now())
  updatedBy            String
  deletedAt            DateTime
  deletedBy            String
  version              Int        @default(1)
  tenant               Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model ErrorLogs {
  id                  String     @id @default(uuid())
  tenantId            String
  correlationId       String
  moduleName          String
  errorSeverity       String     @default("ERROR")
  errorMessage        String
  stackTrace          String
  resolvedStatus      Boolean    @default(false)
  resolvedAt          DateTime
  resolvedByUser      String
  createdAt           DateTime   @default(now())
  createdBy           String
  updatedAt           DateTime   @default(now())
  updatedBy           String
  deletedAt           DateTime
  deletedBy           String
  version             Int        @default(1)
  tenant              Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  resolvedByUserusers Users?     @relation("error_logs_resolved_by_user_to_users", fields: [resolvedByUser], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model SystemConfigurations {
  id          String     @id @default(uuid())
  tenantId    String
  configKey   String
  configValue String
  description String
  createdAt   DateTime   @default(now())
  createdBy   String
  updatedAt   DateTime   @default(now())
  updatedBy   String
  deletedAt   DateTime
  deletedBy   String
  version     Int        @default(1)
  tenant      Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)

  @@unique([tenantId, id])
}

model PlatformEvents {
  id                   String     @id @default(uuid())
  tenantId             String
  eventTopic           String
  eventPayload         Json
  triggeredByUser      String
  dispatchedAt         DateTime   @default(now())
  createdAt            DateTime   @default(now())
  createdBy            String
  updatedAt            DateTime   @default(now())
  updatedBy            String
  deletedAt            DateTime
  deletedBy            String
  version              Int        @default(1)
  tenant               Institutes @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  triggeredByUserusers Users?     @relation("platform_events_triggered_by_user_to_users", fields: [triggeredByUser], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model PlatformAuditLogs {
  id               String                 @id @default(uuid())
  tenantId         String
  eventType        PlatformAuditEventEnum
  description      String
  payload          Json
  triggeredBy      String
  occurredAt       DateTime               @default(now())
  createdAt        DateTime               @default(now())
  createdBy        String
  updatedAt        DateTime               @default(now())
  updatedBy        String
  deletedAt        DateTime
  deletedBy        String
  version          Int                    @default(1)
  tenant           Institutes             @relation("tenant_id_to_institutes", fields: [tenantId], references: [id], onDelete: Restrict)
  triggeredByusers Users?                 @relation("platform_audit_logs_triggered_by_to_users", fields: [triggeredBy], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model MenuGroups {
  id              String       @id @default(uuid())
  tenantId        String
  code            String
  slug            String
  title           String
  icon            String
  color           String
  description     String
  defaultExpanded Boolean      @default(false)
  displayOrder    Int          @default(1)
  isVisible       Boolean      @default(true)
  isSystem        Boolean      @default(false)
  createdAt       DateTime     @default(now())
  updatedAt       DateTime     @default(now())
  createdBy       String
  updatedBy       String
  deletedAt       DateTime
  deletedBy       String
  version         Int          @default(1)
  metadata        Json
  menu_masters    MenuMaster[] @relation("menu_master_menu_group_id_to_menu_groups")

  @@unique([code])
  @@unique([slug])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
  @@unique([tenantId, slug])
  @@index([tenantId, displayOrder, isVisible])
}

model MenuMaster {
  id                      String               @id @default(uuid())
  tenantId                String
  menuGroupId             String
  parentMenuId            String
  menuCode                String
  title                   String
  route                   String
  icon                    String
  displayOrder            Int                  @default(1)
  featureId               String
  module                  String
  pageType                String               @default("PAGE")
  isVisible               Boolean              @default(true)
  isSystem                Boolean              @default(false)
  licenseKey              String
  featureFlagKey          String
  cacheKey                String
  workflowEnabled         Boolean              @default(false)
  workflowKey             String
  createdAt               DateTime             @default(now())
  updatedAt               DateTime             @default(now())
  createdBy               String
  updatedBy               String
  deletedAt               DateTime
  deletedBy               String
  version                 Int                  @default(1)
  metadata                Json
  menu_permissionss       MenuPermissions[]    @relation("menu_permissions_menu_id_to_menu_master")
  menu_masters            MenuMaster[]         @relation("menu_master_parent_menu_id_to_menu_master")
  role_menu_visibilitys   RoleMenuVisibility[] @relation("role_menu_visibility_menu_id_to_menu_master")
  menuGroupIdmenu_groups  MenuGroups?          @relation("menu_master_menu_group_id_to_menu_groups", fields: [menuGroupId], references: [id], onDelete: SetNull)
  parentMenuIdmenu_master MenuMaster?          @relation("menu_master_parent_menu_id_to_menu_master", fields: [parentMenuId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@index([tenantId, menuGroupId, parentMenuId, displayOrder])
}

model PermissionGroups {
  id           String        @id @default(uuid())
  tenantId     String
  code         String
  title        String
  displayOrder Int           @default(1)
  isVisible    Boolean       @default(true)
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @default(now())
  createdBy    String
  updatedBy    String
  deletedAt    DateTime
  deletedBy    String
  version      Int           @default(1)
  metadata     Json
  permissionss Permissions[] @relation("permissions_permission_group_id_to_permission_groups")

  @@unique([code])
  @@unique([tenantId, id])
  @@unique([tenantId, code])
  @@index([tenantId, displayOrder])
}

model RoleMenuVisibility {
  id                String      @id @default(uuid())
  tenantId          String
  roleId            String
  menuId            String
  visibilityMode    String      @default("ALLOW")
  createdAt         DateTime    @default(now())
  updatedAt         DateTime    @default(now())
  createdBy         String
  updatedBy         String
  deletedAt         DateTime
  deletedBy         String
  version           Int         @default(1)
  metadata          Json
  roleIdroles       Roles?      @relation("role_menu_visibility_role_id_to_roles", fields: [roleId], references: [id], onDelete: SetNull)
  menuIdmenu_master MenuMaster? @relation("role_menu_visibility_menu_id_to_menu_master", fields: [menuId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@index([tenantId, roleId])
}

model RoleTemplates {
  id                      String       @id @default(uuid())
  tenantId                String
  roleCode                String
  permissionId            String
  createdAt               DateTime     @default(now())
  updatedAt               DateTime     @default(now())
  createdBy               String
  updatedBy               String
  deletedAt               DateTime
  deletedBy               String
  version                 Int          @default(1)
  metadata                Json
  permissionIdpermissions Permissions? @relation("role_templates_permission_id_to_permissions", fields: [permissionId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@index([tenantId, roleCode])
}

model PermissionDependencies {
  id                         String       @id @default(uuid())
  tenantId                   String
  permissionId               String
  requiresPermissionId       String
  createdAt                  DateTime     @default(now())
  updatedAt                  DateTime     @default(now())
  createdBy                  String
  updatedBy                  String
  deletedAt                  DateTime
  deletedBy                  String
  version                    Int          @default(1)
  metadata                   Json
  permissionIdpermissions    Permissions? @relation("permission_dependencies_permission_id_to_permissions", fields: [permissionId], references: [id], onDelete: SetNull)
  requiresPermisspermissions Permissions? @relation("permission_dependencies_requires_permission_id_to_permissions", fields: [requiresPermissionId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@index([tenantId, permissionId])
}

model RoleInheritance {
  id                  String   @id @default(uuid())
  tenantId            String
  roleId              String
  inheritsRoleId      String
  createdAt           DateTime @default(now())
  updatedAt           DateTime @default(now())
  createdBy           String
  updatedBy           String
  deletedAt           DateTime
  deletedBy           String
  version             Int      @default(1)
  metadata            Json
  roleIdroles         Roles?   @relation("role_inheritance_role_id_to_roles", fields: [roleId], references: [id], onDelete: SetNull)
  inheritsRoleIdroles Roles?   @relation("role_inheritance_inherits_role_id_to_roles", fields: [inheritsRoleId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@index([tenantId, roleId])
}

model PermissionConditions {
  id                             String           @id @default(uuid())
  tenantId                       String
  rolePermissionId               String
  conditionType                  String
  conditionRule                  Json
  createdAt                      DateTime         @default(now())
  updatedAt                      DateTime         @default(now())
  createdBy                      String
  updatedBy                      String
  deletedAt                      DateTime
  deletedBy                      String
  version                        Int              @default(1)
  metadata                       Json
  rolePermissionIrole_permission RolePermissions? @relation("permission_conditions_role_permission_id_to_role_permissions", fields: [rolePermissionId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@index([tenantId, rolePermissionId])
}

model Workflows {
  id                    String                @id @default(uuid())
  tenantId              String
  workflowCode          String
  workflowVersion       Int                   @default(1)
  name                  String
  description           String
  allowParallel         Boolean               @default(false)
  allowCancel           Boolean               @default(true)
  autoComplete          Boolean               @default(false)
  isActive              Boolean               @default(true)
  createdAt             DateTime              @default(now())
  createdBy             String
  updatedAt             DateTime              @default(now())
  updatedBy             String
  deletedAt             DateTime
  deletedBy             String
  version               Int                   @default(1)
  metadata              Json
  workflow_stepss       WorkflowSteps[]       @relation("workflow_steps_workflow_id_to_workflows")
  workflow_requestss    WorkflowRequests[]    @relation("workflow_requests_workflow_id_to_workflows")
  workflow_transitionss WorkflowTransitions[] @relation("workflow_transitions_workflow_id_to_workflows")

  @@unique([tenantId, id])
  @@unique([tenantId, workflowCode, workflowVersion])
  @@index([tenantId, isActive])
}

model WorkflowStates {
  code                           String                @id
  name                           String
  isInitial                      Boolean               @default(false)
  isTerminal                     Boolean               @default(false)
  color                          String
  icon                           String
  createdAt                      DateTime              @default(now())
  workflow_requestss             WorkflowRequests[]    @relation("workflow_requests_status_to_workflow_states")
  workflow_historys              WorkflowHistory[]     @relation("workflow_history_from_status_to_workflow_states")
  workflow_history_to_status     WorkflowHistory[]     @relation("workflow_history_to_status_to_workflow_states")
  workflow_transitionss          WorkflowTransitions[] @relation("workflow_transitions_from_status_to_workflow_states")
  workflow_transitions_to_status WorkflowTransitions[] @relation("workflow_transitions_to_status_to_workflow_states")

  @@unique([code])
}

model WorkflowActions {
  code                  String                @id
  name                  String
  actionCategory        String                @default("USER")
  description           String
  createdAt             DateTime              @default(now())
  workflow_historys     WorkflowHistory[]     @relation("workflow_history_action_code_to_workflow_actions")
  workflow_transitionss WorkflowTransitions[] @relation("workflow_transitions_action_code_to_workflow_actions")

  @@unique([code])
}

model WorkflowSteps {
  id                                        String                @id @default(uuid())
  tenantId                                  String
  workflowId                                String
  stepOrder                                 Int
  stepName                                  String
  stepType                                  String                @default("APPROVAL")
  isOptional                                Boolean               @default(false)
  allowSkip                                 Boolean               @default(false)
  requiredPermission                        String
  assignmentStrategy                        String                @default("PERMISSION")
  assignmentConfig                          Json
  timeoutHours                              Int                   @default(0)
  escalationStrategy                        String                @default("NEXT_APPROVER")
  escalationStepId                          String
  notificationTemplate                      String
  createdAt                                 DateTime              @default(now())
  createdBy                                 String
  updatedAt                                 DateTime              @default(now())
  updatedBy                                 String
  deletedAt                                 DateTime
  deletedBy                                 String
  version                                   Int                   @default(1)
  metadata                                  Json
  workflow_stepss                           WorkflowSteps[]       @relation("workflow_steps_escalation_step_id_to_workflow_steps")
  workflow_requestss                        WorkflowRequests[]    @relation("workflow_requests_current_step_id_to_workflow_steps")
  workflow_historys                         WorkflowHistory[]     @relation("workflow_history_from_step_id_to_workflow_steps")
  workflow_history_to_step_id               WorkflowHistory[]     @relation("workflow_history_to_step_id_to_workflow_steps")
  workflow_commentss                        WorkflowComments[]    @relation("workflow_comments_step_id_to_workflow_steps")
  workflow_escalationss                     WorkflowEscalations[] @relation("workflow_escalations_escalated_from_step_id_to_workflow_steps")
  workflow_escalations_escalated_to_step_id WorkflowEscalations[] @relation("workflow_escalations_escalated_to_step_id_to_workflow_steps")
  workflowIdworkflows                       Workflows?            @relation("workflow_steps_workflow_id_to_workflows", fields: [workflowId], references: [id], onDelete: SetNull)
  escalationStepIdworkflow_steps            WorkflowSteps?        @relation("workflow_steps_escalation_step_id_to_workflow_steps", fields: [escalationStepId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([tenantId, workflowId, stepOrder])
}

model WorkflowRequests {
  id                                  String                            @id @default(uuid())
  tenantId                            String
  workflowId                          String
  workflowVersion                     Int                               @default(1)
  entityType                          String
  entityId                            String
  entityVersion                       Int                               @default(1)
  entitySnapshot                      Json
  currentStepId                       String
  status                              String                            @default("PENDING")
  priority                            String                            @default("NORMAL")
  initiatorId                         String
  currentAssigneeUserId               String
  currentAssigneeGroup                String
  startedAt                           DateTime                          @default(now())
  completedAt                         DateTime
  cancelledAt                         DateTime
  deadlineAt                          DateTime
  createdAt                           DateTime                          @default(now())
  createdBy                           String
  updatedAt                           DateTime                          @default(now())
  updatedBy                           String
  deletedAt                           DateTime
  deletedBy                           String
  version                             Int                               @default(1)
  metadata                            Json
  workflow_historys                   WorkflowHistory[]                 @relation("workflow_history_request_id_to_workflow_requests")
  workflow_commentss                  WorkflowComments[]                @relation("workflow_comments_request_id_to_workflow_requests")
  workflow_attachmentss               WorkflowAttachments[]             @relation("workflow_attachments_request_id_to_workflow_requests")
  workflow_notificationss             WorkflowNotifications[]           @relation("workflow_notifications_request_id_to_workflow_requests")
  workflow_escalationss               WorkflowEscalations[]             @relation("workflow_escalations_request_id_to_workflow_requests")
  workflow_notification_dead_letterss WorkflowNotificationDeadLetters[] @relation("workflow_notification_dead_letters_request_id_to_workflow_requests")
  workflowIdworkflows                 Workflows?                        @relation("workflow_requests_workflow_id_to_workflows", fields: [workflowId], references: [id], onDelete: SetNull)
  currentStepIdworkflow_steps         WorkflowSteps?                    @relation("workflow_requests_current_step_id_to_workflow_steps", fields: [currentStepId], references: [id], onDelete: SetNull)
  statusworkflow_states               WorkflowStates?                   @relation("workflow_requests_status_to_workflow_states", fields: [status], references: [code], onDelete: SetNull)

  @@unique([tenantId, id])
  @@index([tenantId, entityType, entityId])
}

model WorkflowHistory {
  id                         String            @id @default(uuid())
  tenantId                   String
  requestId                  String
  fromStepId                 String
  toStepId                   String
  actionCode                 String
  fromStatus                 String
  toStatus                   String
  fromAssigneeUserId         String
  toAssigneeUserId           String
  fromAssigneeGroup          String
  toAssigneeGroup            String
  performedBy                String
  notes                      String
  createdAt                  DateTime          @default(now())
  metadata                   Json
  requestIdworkflow_requests WorkflowRequests? @relation("workflow_history_request_id_to_workflow_requests", fields: [requestId], references: [id], onDelete: SetNull)
  fromStepIdworkflow_steps   WorkflowSteps?    @relation("workflow_history_from_step_id_to_workflow_steps", fields: [fromStepId], references: [id], onDelete: SetNull)
  toStepIdworkflow_steps     WorkflowSteps?    @relation("workflow_history_to_step_id_to_workflow_steps", fields: [toStepId], references: [id], onDelete: SetNull)
  actionCodeworkflow_actions WorkflowActions?  @relation("workflow_history_action_code_to_workflow_actions", fields: [actionCode], references: [code], onDelete: SetNull)
  fromStatusworkflow_states  WorkflowStates?   @relation("workflow_history_from_status_to_workflow_states", fields: [fromStatus], references: [code], onDelete: SetNull)
  toStatusworkflow_states    WorkflowStates?   @relation("workflow_history_to_status_to_workflow_states", fields: [toStatus], references: [code], onDelete: SetNull)

  @@unique([tenantId, id])
}

model WorkflowComments {
  id                         String            @id @default(uuid())
  tenantId                   String
  requestId                  String
  stepId                     String
  userId                     String
  commentText                String
  commentType                String            @default("GENERAL")
  visibility                 String            @default("PUBLIC")
  createdAt                  DateTime          @default(now())
  createdBy                  String
  updatedAt                  DateTime          @default(now())
  updatedBy                  String
  deletedAt                  DateTime
  deletedBy                  String
  version                    Int               @default(1)
  metadata                   Json
  requestIdworkflow_requests WorkflowRequests? @relation("workflow_comments_request_id_to_workflow_requests", fields: [requestId], references: [id], onDelete: SetNull)
  stepIdworkflow_steps       WorkflowSteps?    @relation("workflow_comments_step_id_to_workflow_steps", fields: [stepId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model WorkflowAttachments {
  id                         String            @id @default(uuid())
  tenantId                   String
  requestId                  String
  fileName                   String
  storageKey                 String
  mimeType                   String
  size                       Int
  uploadedBy                 String
  createdAt                  DateTime          @default(now())
  createdBy                  String
  updatedAt                  DateTime          @default(now())
  updatedBy                  String
  deletedAt                  DateTime
  deletedBy                  String
  version                    Int               @default(1)
  metadata                   Json
  requestIdworkflow_requests WorkflowRequests? @relation("workflow_attachments_request_id_to_workflow_requests", fields: [requestId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
  @@index([tenantId, requestId])
}

model WorkflowNotifications {
  id                         String            @id @default(uuid())
  tenantId                   String
  requestId                  String
  recipientUserId            String
  notificationType           String            @default("EMAIL")
  title                      String
  body                       String
  status                     String            @default("PENDING")
  retryCount                 Int               @default(0)
  maxRetry                   Int               @default(3)
  lastAttempt                DateTime
  nextRetryAt                DateTime
  errorLog                   String
  createdAt                  DateTime          @default(now())
  processedAt                DateTime
  deletedAt                  DateTime
  deletedBy                  String
  version                    Int               @default(1)
  metadata                   Json
  requestIdworkflow_requests WorkflowRequests? @relation("workflow_notifications_request_id_to_workflow_requests", fields: [requestId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model WorkflowDelegations {
  id             String   @id @default(uuid())
  tenantId       String
  originalUserId String
  delegateUserId String
  effectiveFrom  DateTime @default(now())
  effectiveTo    DateTime
  delegationType String   @default("TEMPORARY")
  reason         String
  isActive       Boolean  @default(true)
  createdAt      DateTime @default(now())
  createdBy      String
  updatedAt      DateTime @default(now())
  updatedBy      String
  deletedAt      DateTime
  deletedBy      String
  version        Int      @default(1)
  metadata       Json

  @@unique([tenantId, id])
}

model WorkflowEscalations {
  id                            String            @id @default(uuid())
  tenantId                      String
  requestId                     String
  escalatedFromStepId           String
  escalatedToStepId             String
  escalationLevel               Int               @default(1)
  targetStrategy                String
  afterMinutes                  Int
  escalatedAt                   DateTime          @default(now())
  executedAt                    DateTime
  executedBy                    String
  createdAt                     DateTime          @default(now())
  createdBy                     String
  updatedAt                     DateTime          @default(now())
  updatedBy                     String
  deletedAt                     DateTime
  deletedBy                     String
  version                       Int               @default(1)
  metadata                      Json
  requestIdworkflow_requests    WorkflowRequests? @relation("workflow_escalations_request_id_to_workflow_requests", fields: [requestId], references: [id], onDelete: SetNull)
  escalatedFromStworkflow_steps WorkflowSteps?    @relation("workflow_escalations_escalated_from_step_id_to_workflow_steps", fields: [escalatedFromStepId], references: [id], onDelete: SetNull)
  escalatedToStepworkflow_steps WorkflowSteps?    @relation("workflow_escalations_escalated_to_step_id_to_workflow_steps", fields: [escalatedToStepId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model WorkflowTransitions {
  id                         String           @id @default(uuid())
  tenantId                   String
  workflowId                 String
  fromStatus                 String
  toStatus                   String
  actionCode                 String
  requiresComment            Boolean          @default(false)
  conditionExpression        String
  eventName                  String
  transitionName             String
  createdAt                  DateTime         @default(now())
  createdBy                  String
  updatedAt                  DateTime         @default(now())
  updatedBy                  String
  deletedAt                  DateTime
  deletedBy                  String
  version                    Int              @default(1)
  metadata                   Json
  workflowIdworkflows        Workflows?       @relation("workflow_transitions_workflow_id_to_workflows", fields: [workflowId], references: [id], onDelete: SetNull)
  fromStatusworkflow_states  WorkflowStates?  @relation("workflow_transitions_from_status_to_workflow_states", fields: [fromStatus], references: [code], onDelete: SetNull)
  toStatusworkflow_states    WorkflowStates?  @relation("workflow_transitions_to_status_to_workflow_states", fields: [toStatus], references: [code], onDelete: SetNull)
  actionCodeworkflow_actions WorkflowActions? @relation("workflow_transitions_action_code_to_workflow_actions", fields: [actionCode], references: [code], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([tenantId, workflowId, fromStatus, actionCode, toStatus])
}

model WorkflowNotificationDeadLetters {
  id                         String            @id @default(uuid())
  tenantId                   String
  requestId                  String
  originalNotificationId     String
  recipientUserId            String
  notificationType           String            @default("EMAIL")
  title                      String
  body                       String
  retryCount                 Int               @default(0)
  maxRetry                   Int               @default(3)
  lastError                  String
  deadAt                     DateTime          @default(now())
  createdAt                  DateTime          @default(now())
  metadata                   Json
  requestIdworkflow_requests WorkflowRequests? @relation("workflow_notification_dead_letters_request_id_to_workflow_requests", fields: [requestId], references: [id], onDelete: SetNull)

  @@unique([tenantId, id])
}

model PolicyCategories {
  code             String           @id
  name             String
  description      String
  displayOrder     Int              @default(0)
  isActive         Boolean          @default(true)
  createdAt        DateTime         @default(now())
  createdBy        String
  updatedAt        DateTime         @default(now())
  updatedBy        String
  deletedAt        DateTime
  deletedBy        String
  version          Int              @default(1)
  metadata         Json
  policy_settingss PolicySettings[] @relation("policy_settings_category_code_to_policy_categories")

  @@unique([code])
}

model PolicySettings {
  id                            String            @id @default(uuid())
  tenantId                      String
  categoryCode                  String
  policyKey                     String
  value                         Json
  valueType                     String            @default("STRING")
  defaultValue                  Json
  validationRule                Json
  description                   String
  isSystem                      Boolean           @default(false)
  isEditable                    Boolean           @default(true)
  tenantOverride                Boolean           @default(true)
  createdAt                     DateTime          @default(now())
  createdBy                     String
  updatedAt                     DateTime          @default(now())
  updatedBy                     String
  deletedAt                     DateTime
  deletedBy                     String
  version                       Int               @default(1)
  metadata                      Json
  categoryCodepolicy_categories PolicyCategories? @relation("policy_settings_category_code_to_policy_categories", fields: [categoryCode], references: [code], onDelete: SetNull)

  @@unique([tenantId, id])
  @@unique([tenantId, policyKey])
}

model AuditLogs {
  id            BigInt   @default(autoincrement())
  tenantId      String
  entityType    String
  entityId      String
  action        String
  oldValue      Json
  newValue      Json
  performedBy   String
  ipAddress     String
  userAgent     String
  requestId     String
  correlationId String
  createdAt     DateTime @default(now())
  metadata      Json

  @@unique([tenantId, id])
}

model SystemSettings {
  id          String   @id @default(uuid())
  settingKey  String   @unique
  value       Json
  valueType   String   @default("STRING")
  description String
  isEncrypted Boolean  @default(false)
  isPublic    Boolean  @default(false)
  createdAt   DateTime @default(now())
  createdBy   String
  updatedAt   DateTime @default(now())
  updatedBy   String
  deletedAt   DateTime
  deletedBy   String
  version     Int      @default(1)
  metadata    Json
}

model Countries {
  id                     String                @id @default(uuid())
  iso2                   String                @unique
  iso3                   String                @unique
  numericCode            Int                   @unique
  name                   String
  nativeName             String
  phoneCode              String
  currencyCode           String
  continent              String
  flagEmoji              String
  isActive               Boolean               @default(true)
  displayOrder           Int                   @default(0)
  createdAt              DateTime              @default(now())
  updatedAt              DateTime              @default(now())
  version                Int                   @default(1)
  metadata               Json
  statess                States[]              @relation("states_country_id_to_countries")
  localess               Locales[]             @relation("locales_country_iso2_to_countries")
  countries_phone_codess CountriesPhoneCodes[] @relation("countries_phone_codes_country_id_to_countries")
}

model States {
  id                 String     @id @default(uuid())
  countryId          String
  code               String
  name               String
  type               String     @default("STATE")
  isActive           Boolean    @default(true)
  displayOrder       Int        @default(0)
  createdAt          DateTime   @default(now())
  updatedAt          DateTime   @default(now())
  version            Int        @default(1)
  metadata           Json
  countryIdcountries Countries? @relation("states_country_id_to_countries", fields: [countryId], references: [id], onDelete: SetNull)

  @@unique([code])
  @@unique([countryId, code])
}

model Languages {
  code         String    @id
  iso6391      String
  iso6392      String
  name         String
  nativeName   String
  isRtl        Boolean   @default(false)
  isActive     Boolean   @default(true)
  displayOrder Int       @default(0)
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @default(now())
  version      Int       @default(1)
  metadata     Json
  localess     Locales[] @relation("locales_language_code_to_languages")

  @@unique([code])
  @@unique([iso6391])
  @@unique([iso6392])
}

model Currencies {
  code          String   @id
  numericCode   Int      @unique
  name          String
  symbol        String
  symbolNative  String
  decimalPlaces Int      @default(2)
  isDefault     Boolean  @default(false)
  isActive      Boolean  @default(true)
  displayOrder  Int      @default(0)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @default(now())
  version       Int      @default(1)
  metadata      Json

  @@unique([code])
}

model Timezones {
  id               String   @id @default(uuid())
  name             String   @unique
  utcOffset        String
  utcOffsetMinutes Int
  observesDst      Boolean  @default(false)
  isActive         Boolean  @default(true)
  displayOrder     Int      @default(0)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @default(now())
  version          Int      @default(1)
  metadata         Json
}

model Locales {
  code                  String     @id
  languageCode          String
  countryIso2           String
  name                  String
  isActive              Boolean    @default(true)
  displayOrder          Int        @default(0)
  createdAt             DateTime   @default(now())
  updatedAt             DateTime   @default(now())
  version               Int        @default(1)
  metadata              Json
  languageCodelanguages Languages? @relation("locales_language_code_to_languages", fields: [languageCode], references: [code], onDelete: SetNull)
  countryIso2countries  Countries? @relation("locales_country_iso2_to_countries", fields: [countryIso2], references: [id], onDelete: SetNull)

  @@unique([code])
  @@unique([languageCode, countryIso2])
}

model FileTypes {
  code                  String      @id
  name                  String
  description           String
  maxSizeBytes          BigInt
  allowedMimeCategories String[]
  isActive              Boolean     @default(true)
  displayOrder          Int         @default(0)
  createdAt             DateTime    @default(now())
  updatedAt             DateTime    @default(now())
  version               Int         @default(1)
  metadata              Json
  mime_typess           MimeTypes[] @relation("mime_types_file_type_code_to_file_types")

  @@unique([code])
}

model MimeTypes {
  id                     String     @id @default(uuid())
  mimeType               String     @unique
  fileTypeCode           String
  extension              String
  category               String
  isActive               Boolean    @default(true)
  createdAt              DateTime   @default(now())
  updatedAt              DateTime   @default(now())
  version                Int        @default(1)
  metadata               Json
  fileTypeCodefile_types FileTypes? @relation("mime_types_file_type_code_to_file_types", fields: [fileTypeCode], references: [code], onDelete: SetNull)
}

model StorageProviders {
  code         String   @id
  name         String
  description  String
  configSchema Json
  priority     Int      @default(0)
  isActive     Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @default(now())
  version      Int      @default(1)
  metadata     Json

  @@unique([code])
}

model ErrorCodes {
  code       String   @id
  module     String
  message    String
  detail     String
  httpStatus Int      @default(400)
  isActive   Boolean  @default(true)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @default(now())
  version    Int      @default(1)
  metadata   Json

  @@unique([code])
}

model UnitsOfMeasure {
  code             String   @id
  name             String
  category         String
  symbol           String
  baseUnit         String
  conversionFactor Decimal
  isActive         Boolean  @default(true)
  displayOrder     Int      @default(0)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @default(now())
  version          Int      @default(1)
  metadata         Json

  @@unique([code])
}

model CountriesPhoneCodes {
  id                 String     @id @default(uuid())
  countryId          String
  phoneCode          String
  example            String
  mobileLengthMin    Int
  mobileLengthMax    Int
  landlineLengthMin  Int
  landlineLengthMax  Int
  nationalPrefix     String
  validationRegex    String
  isActive           Boolean    @default(true)
  createdAt          DateTime   @default(now())
  updatedAt          DateTime   @default(now())
  version            Int        @default(1)
  metadata           Json
  countryIdcountries Countries? @relation("countries_phone_codes_country_id_to_countries", fields: [countryId], references: [id], onDelete: SetNull)

  @@unique([countryId, phoneCode])
}

model DateFormats {
  code          String   @id
  name          String
  formatPattern String
  category      String
  exampleOutput String
  isActive      Boolean  @default(true)
  displayOrder  Int      @default(0)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @default(now())
  version       Int      @default(1)
  metadata      Json

  @@unique([code])
}
